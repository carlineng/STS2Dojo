# STS2 Dojo Project

**Purpose of this doc:** the design reference AND running build log for STS2 Dojo. It captures what the mod is, what's been decided, what the run-file data can/can't support, and current status. The §8 spike is **done**; real coding is underway. Read the status block below first, then the rest as reference.

---

## 0. Current status & how to resume (updated 2026-07-04) — READ FIRST

⚠️ **A real incident happened this session, now resolved: the Dojo run browser silently overwrote the user's real (non-modded) profile's `progress.save` with modded-session data, making the Compendium button disappear from their real profile's main menu.** Root cause fixed and merged (`DojoRunHistorySaveSafetyPatch`, confirmed in-game); the real profile's stats were also recovered (reconstructed from intact `.run` history after Steam Cloud/local backups turned out to already be corrupted too). Full mechanism, the recovery method, and — most importantly — **a durable rule for all future work near save/profile code** are in §5h and the new §6 rule. Read both before touching `DojoRunBrowser.cs`, `UserDataPathProvider.IsRunningModded`, or anything that saves profile-scoped data.

The §8 sandbox spike is **complete and validated** (statically + empirically). The reconstructor (§9 roadmap item 1) is **built, empirically validated in-game against a real `.run` file, and now covered by a lightweight unit-test harness**. The first real UI loop is now merged to `main`: a **Dojo main-menu entry point**, a **real-profile Run History browser**, combat-floor click-to-replay, and a **Completion screen** after win/loss. The task-tool list is ephemeral (it resets between/within sessions) — **§9 is the durable roadmap; this block is the durable status.**

**Done this far:**
- **Spike validated — Approach A works.** A `dojo <encounter_id>` dev command launches a fight from a throwaway *non-saving* run and creates **zero run-history entries** on win/abandon/death, real save untouched. Full result + punch-list in §8.0.
- **Dev environment working (macOS 26.5.1, MacBook Air M1):** modded game boots, builds auto-deploy, `dojo` launches fights.
- **Reconstructor built and validated (`STS2Dojo/STS2DojoCode/Reconstruction/`):** `.run` file + global floor → provenance-tagged loadout (deck/relics/HP/gold), wired into a new `dojoreplay <run_file_path> <floor>` dev command. Validated by reconstructing floor 25 of `runfiles/1779595721.run` in-game: 24-card deck (incl. ascension-10 Ascender's Bane), 10 relics, HP 30/70, gold 55 — all confirmed to match the file, deck/relics functionally correct (relics proc, e.g. Tough Bandages triggered on discard). See §5a for what changed vs. the original plan and the bugs this surfaced.
- **RunReconstructor + decision-helper unit tests added (`STS2Dojo/STS2Dojo.Tests/`):** no-NuGet console test harness that compiles the real reconstruction/decision helper source against small DTO test doubles and reads local `runfiles/*.run` fixtures directly. Verified with `/usr/local/share/dotnet/dotnet run --project STS2Dojo.Tests/STS2Dojo.Tests.csproj`: 8 reconstruction fixture tests + 5 decision-helper test groups pass. See §5b.
- **Potion reconstruction implemented (2026-07-01), closing the last "Assumed" field in the loadout contract — including a wrong turn worth reading.** `RunReconstructor` replays `potion_choices`/`potion_used`/`potion_discarded` (structural signals) plus explicit max-potion-slot tracking (Ascension's Tight Belt, Potion Belt/Alchemical Coffer/Phial Holster). An initial version also added event-based fallback grants/removals (Drowning Beacon/Potion Courier/Ranwid the Elder/Stone of All Time), which got merged and unit-tested but turned out to be solving a nonexistent gap and produced real bugs (caught via in-game slot-count mismatch) — deleted; see §5d for the full account, since it's a good example of corpus correlation without checking the actual game logic leading you wrong. `DojoReplayConsoleCmd` now gives the player the reconstructed potions and reconciles slot count to the computed true value. 8 potion acceptance fixtures pass, 3 of them regression tests for the deleted bug — see §5d.
- **Mod code, in `STS2Dojo/STS2DojoCode/`:**
  - `MainFile.cs` — mod initializer (`[ModInitializer]`); runs `new Harmony(ModId).PatchAll()`.
  - `SentryMacOsHangPatch.cs` — **CRITICAL, DO NOT REMOVE.** Harmony prefix that skips `SentryService.Shutdown()` on macOS. Without it the modded game **hard-hangs at the main menu on macOS 26.x** (native Sentry GDExtension run-loop-observer deadlock). Full diagnosis in the `baselib-sentry-macos-hang` memory. (Open TODO: make modded Sentry fully silent — see the `TODO(telemetry)` in that file; currently telemetry is at unmodded parity / gated by the user's data-upload consent.)
  - `DojoConsoleCmd.cs` — `dojo <encounter_id>`: launches a throwaway fight with a hardcoded junk loadout (gold=999). Still useful for quick ad-hoc encounter testing.
  - `DojoLaunch.cs` — the shared launch sequence (`LaunchThrowawayRun`/`TryAgain`), reused by `DojoConsoleCmd`/`DojoReplayConsoleCmd`/`DojoReplayLauncher`/`DojoAgainConsoleCmd`; now also stops stale run/global music before launching/retrying a Dojo fight. See §5c for what it does and why.
  - `DojoReplayConsoleCmd.cs` — `dojoreplay <run_file_path> <floor>`. Loads a `.run` file, reconstructs the loadout entering that floor's fight, and launches it.
  - `DojoReplayLauncher.cs` — shared reconstruction+launch pipeline for the console command and UI floor-click path; resolves the encounter, snapshots the true ascension-adjusted starting inventory, reconstructs the loadout, mutates the throwaway run, and launches via `DojoLaunch`.
  - `DojoAgainConsoleCmd.cs` — `dojoagain`: relaunches the last `dojo`/`dojoreplay` launch with the same loadout and fresh combat RNG ("try again"). See §5c.
  - `DojoMainMenuPatch.cs` — Harmony postfix on `NMainMenu._Ready` that duplicates the themed Settings main-menu button into a new **Dojo** button, inserts it before Settings, and wires it to `NDojoScreen.Open`.
  - `DojoRunBrowser.cs` — **now contains ONLY `DojoRunHistorySaveSafetyPatch`** (the unconditional §5h save-corruption net). The former stock-`NRunHistory`-reuse browser that lived here (the `IsRunningModded = false` flip, `Open`/`OpenAtRun`, the two restore patches, `DojoRunHistoryTargetRunPatch`, and the `_restorePending`/`_targetRunFileName` plumbing) is **gone** as of 2026-07-03: the custom screen reads the real profile directly and the "View All Combats" drill-in was replaced by the in-row floor map (§5i). Nothing in the mod flips `IsRunningModded` anymore; the safety patch stays as a pure last line of defense.
  - `DojoScreen.cs` — **the custom Dojo run-browser screen (`NDojoScreen : NSubmenu`), the Dojo's landing screen since 2026-07-02** — see §5i. Also contains `DojoRunRow` (one run's row, collapsed by default and expandable IN PLACE to the full per-act floor map — the in-row combat view that replaced the "View All Combats" drill-in, 2026-07-03; see §5j), `DojoUi` (game-font lookup + label/chip factories — every code-created label needs an explicit theme font override, `MegaLabel._Ready` throws without one), `DojoChip`/`DojoBackChip`/`DojoFightPill` plus the stock-control-based character icon/token helpers used by the sidebar and run rows.
  - `DojoRunIndex.cs` — read-only loader/cache for the real profile's `.run` files feeding `NDojoScreen`; computes the real history dir by stripping `/modded/` from the current profile base path (never touches `IsRunningModded`), classifies via `RunHistoryFileSelector`, caches summaries by file mtime, evicts deleted files.
  - `DojoDisplayNames.cs` — memoized live display-name lookups (character/encounter/act `Title` via `ModelDb`) with a prettified-id fallback for unresolvable content; backs both row rendering and the search box.
  - `DojoReplaySetupModal.cs` — the "Dojo — Replay Setup" modal (`NDojoReplaySetupModal`), the shared pre-launch screen used by both the stock floor-click patch and the custom screen's fight pills. Replaced the old one-line `DojoReplayConfirmation` yes/no popup 2026-07-03 — see §5n. Also holds the `DojoStepperChip`/`DojoPresetChip` widgets.
  - `DojoStatefulContent.cs` — the registry of every `[SavedProperty]`-stateful relic (40) and card (4): which state is player-editable in the Replay Setup modal (specs with helper text/min/max/primed values), which is deliberately excluded and why, plus `DojoStateAdjustments` (the modal's output, applied to the reconstructed loadout at launch). See §5n.
  - `DojoDeckViewer.cs` — opens the stock `NDeckViewScreen` (the in-combat "D"-key deck viewer) at the main menu for a historical deck (run row's end-of-run deck / saved fight's captured deck), hosted by `NModalContainer` with a throwaway never-registered Player holding the cards (§5a-safe `RunState.LoadCard` + `AddInternal(silent)` sequence); includes `DojoDeckViewClosePatch`, which redirects the Back button away from the null-at-menu capstone container for Dojo-hosted instances only.
  - `DojoRunHistoryFloorClickPatch.cs` — patches `NMapPointHistoryEntry.Create` (the stock floor-icon widget), but only modifies entries created through `DojoFloorClickPatch.CreateDojoEntry` by `DojoRunRow`'s in-row floor map. Stock Run History can still be opened through the Compendium in some modded sessions, so plain `NMapPointHistoryEntry.Create` calls are deliberately left untouched. Eligible Dojo combat floors prompt "Replay this fight in the Dojo?" and launch through `DojoReplayLauncher`; non-combat floors (rest/shop/event/treasure) are left rendered normally; only INELIGIBLE COMBAT floors are greyed out (`StsColors.disabledTopBarButton`) and left unwired. The §5g pre-highlight (`DojoFloorHighlightPatches`) was removed 2026-07-03 — the mock wants uniform icon sizes. See §5g/§5j.
  - `DojoFloorEligibility.cs` — the structural + live content-resolve eligibility check backing the grey-out above (`IsEligible`), plus `IsCombatFloor` (does this floor contain a fight at all — the grey-vs-normal split above); builds a cheap, throwaway (never-registered-with-`RunManager`) `RunState`/`Player` purely to get an accurate starting-deck/relic snapshot for the preview reconstruction, cached per (character, ascension). See §5g/§5j.
  - `DojoCompletionScreen.cs` — code-only modal shown after a Dojo win/loss, with Try Again / Return to Dojo / Return to Main Menu; reuses rewards-screen title/chrome, custom event-option-style buttons, and blocks the game's hover tips while open. Derives from the inert game class `NTopBarPortrait` ON PURPOSE — see §5m before changing its base class.
  - `DojoRunRegistry.cs` — tags which `RunState`s are Dojo (throwaway) runs, so the combat-end/save-suppression patches below never affect a real player run. See §5c.
  - `DojoCombatEndPatches.cs` / `DojoCombatEndInterceptor.cs` — Harmony patches that skip the rewards/map/game-over screens on a Dojo win/loss, suppress the secondary save leaks (`progress.save`, `replays/latest.mcr`), and show `DojoCompletionScreen` instead of redirecting through Run History. See §5c/§5e.
  - `Reconstruction/RunReconstructor.cs` — the forward-replay algorithm (see §5a).
  - `Reconstruction/ReconstructedLoadout.cs` — provenance-tagged output model (`Derived`/`Replayed`/`Assumed`).
  - `Reconstruction/RunHistoryLoader.cs` + `Reconstruction/LocalFileSaveStore.cs` — loads a `.run` file from an arbitrary absolute path by reusing the game's own `RunHistory` schema + `MigrationManager` (see §5a — this replaced the originally-planned hand-rolled parser).
  - `Reconstruction/RunHistoryQueries.cs` — pure decision helpers: combat-floor detection, single-player gating, floor-history flattening. Unit-tested in §5b.
  - `Reconstruction/RunHistoryFileSelection.cs` — classifies a candidate `.run` file (load failure, unsupported schema, multiplayer, modifier-bearing, no-combat, includable) for future custom browser/filter/error-state work (§9 item 5). Unit-tested in §5b. Already resolves the §10 daily-run/modifier-run question: modifier-bearing runs are excluded by default (`ExcludeModifierRuns = true`).
  - `Reconstruction/DojoContentEligibility.cs` — the pure eligibility-check logic (§6) behind an `IDojoContentResolver` seam; collects missing encounter/monster/card/relic/potion ids from a loadout, plus `DojoContentEligibilityException`. Unit-tested in §5b.
  - `Reconstruction/LiveDojoContentResolver.cs` — the live `ModelDb`-backed `IDojoContentResolver`, wired into `DojoReplayLauncher` so an ineligible replay is refused with a clear message instead of a raw `ModelNotFoundException`. See §5f.
  - `Reconstruction/RealProfilePath.cs` — builds the real (non-modded) profile's `.run` history path; now actually used at runtime by `DojoRunIndex` (strip-`/modded/` variant), realizing the "future custom browser work" it was built for.
  - `Reconstruction/DojoRunSummary.cs` — pure per-run row model + `DojoRunSummarizer` (character/ascension/win/floors/end-HP/date/duration/seed/deck+relic counts, per-act display fights with death-fight marking, including a final hallway death fight when applicable, lazy weak-cached `RunSource` so ~1000 summaries don't pin ~1000 parsed run graphs). Unit-tested (§5b/§5i).
  - `Reconstruction/DojoRunListQueries.cs` — pure filter (character / exact ascension / victory / search over character+encounter+monster names+seed via a name-resolver seam) + sort (newest/oldest/floor/ascension) for the custom browser. Unit-tested.
- **Relic-visibility fix + combat-end interception built AND empirically confirmed in-game** (branch `fix/relic-visibility-combat-end`, commits `0c58ec0`/`c74b58b`; later extended by PR #3): `DojoLaunch` no longer calls `NGame.StartNewSingleplayerRun`; it replicates the raw sequence manually so the loadout mutation runs *before* `NRun`'s scene is created — this fixes the previously-invisible relic icons (§5a point 6 is now **RESOLVED**; §5c has what shipped). Combat-end is Harmony-patched (`DojoCombatEndPatches.cs`): a Dojo win/loss skips the rewards screen/map/game-over screen and now shows `DojoCompletionScreen` directly over the still-alive combat scene. A `dojoagain` console command still exists for quick dev-loop retries, but the player-facing Try Again button now uses the same `DojoLaunch.TryAgain()` path. `DojoRunRegistry` tags Dojo `RunState`s so none of this touches a real player run. See §5c/§5e for full detail, including two robustness bugs a code review caught (a failed `dojoreplay` mutation could permanently occupy `RunManager.State`; the registry leaked every `RunState` for the process lifetime — both fixed) and the secondary-write suppression status.
- **Dojo entry point + run browser + Completion screen merged to `main` via PR #3 (2026-07-02).** The modded main menu now has a **Dojo** button; the Dojo reuses the game's `NRunHistory` screen while temporarily pointing save/history lookups at the real profile; eligible combat floors prompt and launch a replay; win/loss opens a centered Completion screen with Try Again / Return to Dojo / Return to Main Menu. This resolves the old design gap where combat-end redirected straight to Run History. A full in-game smoke pass through the whole loop (main menu → run history → floor confirm → replay → win/loss → all three Completion-screen buttons, plus the macOS screenshot `<null>`-tooltip check) has since been done manually and passed (2026-07-02). See §5e and §9.
- **Live eligibility/resolve pass wired into replay launches (2026-07-02).** `LiveDojoContentResolver` checks a reconstructed loadout's encounter/monster/card/relic/potion ids against the currently-loaded `ModelDb`; `DojoReplayLauncher` calls `DojoContentEligibility.Validate(...)` right after reconstruction and before any player mutation, throwing `DojoContentEligibilityException` (caught and logged clearly, not as a raw stack trace) on a miss. Closes §9 roadmap item 4's remaining gap. See §5f.
- **Ineligible floors greyed out in the run browser (2026-07-02).** `DojoFloorEligibility` runs the same content-resolve check up front, using a cheap throwaway `RunState`/`Player` (never registered with `RunManager`, no asset/scene work) instead of the full launch sequence. `DojoFloorClickPatch` dims (`StsColors.disabledTopBarButton`) and leaves unwired any floor that fails structural or content eligibility, so ineligible floors are visibly not pickable before a click, not just refused after. See §5g. Not yet exercised in-game.
- ⚠️ **Real-profile save corruption incident — fixed AND recovered (2026-07-02) — see §5h + the new §6 rule, read before touching `DojoRunBrowser.cs`.** User-reported: after disabling mods and restarting, their real profile's Compendium button vanished. Root cause: `DojoRunBrowser`'s `UserDataPathProvider.IsRunningModded = false` flip (used to redirect the run browser at the real profile) could still be `false` when the game quit/restarted or when Settings closed independently, and `SaveManager.SaveProgressFile`/`SaveProfile`/`SavePrefsFile` resolve their write PATH live from that flag while always serializing the modded session's in-memory data — silently overwriting the real profile's `progress.save` with near-empty modded-session stats. Fixed via `DojoRunHistorySaveSafetyPatch` (confirmed in-game after the fact). The real profile's stats were also recovered — reconstructed from the intact `.run` history after discovering the local `.backup` and Steam Cloud copies were already corrupted too; see §5h's recovery bullet for the method and the Steam Cloud sync fight that came with it.
- **Custom Dojo run-browser screen built (2026-07-02, branch `feature/dojo-custom-run-browser`).** Replaces the stock-`NRunHistory`-reuse landing screen with `NDojoScreen` (§5i): sidebar filters (character/ascension/victory/search/sort), a scrolling list of the real profile's single-player runs (read directly from disk — **no `IsRunningModded` flip for the run list at all**), per-act boss/elite pills that launch a reconstructed fight via `DojoReplayLauncher` (eligibility-gated + greyed out). Pure logic (summaries/per-act extraction/filtering/sorting/search) unit-tested with 6 new test groups. A self-review pass fixed 10 findings pre-merge, incl. a real ESC-hotkey-shadowing bug and a ~100s-of-MB memory pin — see §5i.
- **In-row combat view + full removal of the `IsRunningModded` flip (2026-07-03) — build-verified, unit-tested, and now smoke-tested in-game 2026-07-03 (§5l).** The per-row "View All Combats" drill-in to the stock `NRunHistory` (the last thing that flipped `UserDataPathProvider.IsRunningModded = false`) is replaced by an in-place EXPAND: each `DojoRunRow` toggles between its collapsed summary and the full per-act floor map, built by reusing the stock `NMapPointHistoryEntry.Create` widget through the Dojo-only `DojoFloorClickPatch.CreateDojoEntry` helper (room icons and per-floor hover tooltip come from the stock widget; grey-out and click-to-replay are applied only to those Dojo-owned entries). With the drill-in gone, `DojoRunBrowser.cs` was gutted to just `DojoRunHistorySaveSafetyPatch`; `Open`/`OpenAtRun`, the two restore patches, `DojoRunHistoryTargetRunPatch`, and the `_restorePending`/`_targetRunFileName` plumbing were deleted, and `DojoFloorHighlightPatches`/`EligibleEntries` (the §5g pre-highlight) were dropped since the mock wants uniform icon sizes. **A repo-wide grep confirms nothing writes `IsRunningModded = false` anymore** — the safety patch only ever forces it `true`. The §5h corruption vector (a flip left `false` across a save) is now structurally impossible, not just guarded.
- **"Dojo — Replay Setup" modal built (2026-07-03, branch `dojo-replay-setup`) — merged to `main` via PR #12, and now in-game smoke-tested and PASSED (2026-07-04).** Replaces the one-line "Replay this fight?" confirm popup with a pre-launch setup screen: summary strip (encounter/floor/character/ascension + HP/Gold/Potion chips from a preview reconstruction), and an editor over the previously-`assumed` relic counter/card state — one row per stateful relic/card in the loadout, steppers + Zero/Random/Primed presets, values applied at launch through the game's own `SavedProperties` restore pipeline. Realizes the relic/card-state half of §9 item 6. See §5n for the design and the registry coverage (29 of 40 stateful relics + 3 of 4 stateful cards editable; exclusions documented) — the in-game smoke checklist there is now checked off.
- **Repeatable-seed fight sharing (§12) DONE (branch `replayable-seed`, merged PR #15) — build green, in-game smoke test PASSED (export + import/paste + same-seed determinism, 2026-07-04/05).** Full §12 loop: every Dojo launch is now deterministic-capture-ready (encounter RNG derived from the run seed instead of wall clock; `UnlockState.all` pools; per-attempt snapshot capture in `DojoLaunch`), Export from the Completion screen and the Replay Setup modal (library file + `STS2DOJO1.` clipboard code), a Saved Fights tab on `NDojoScreen`, and seed-pinned imported launches where Try Again replays identically. Two §12a determinism gaps were found during pre-implementation review and fixed (see §12i, plus the work log `../SEED_SHARING_PROGRESS.md` OUTSIDE the repo — read §12i's file list before touching any of this). **Saved Fights UI then substantially reworked (merged PRs #16/#17/#18, in-game confirmed) — export split (copy vs save), always-loaded rich rows with avatars/relic bar, per-row edit/delete/copy, explicit Submit + newest-import highlight, search + character/ascension filters, and an author field. Read §12j before touching the Saved Fights screen — it supersedes the §12i UI notes.**
- **View Deck + deck-card search (2026-07-05, branch `feature/view-deck-and-card-search`) — build green, unit tests pass; in-game smoke test PENDING.** Both Dojo tabs can now open the game's own D-key deck viewer (`NDeckViewScreen`) from the main menu: each Run History row gets a "View Deck" chip (the run's end-of-run deck, re-read from the run file so upgrade levels/enchantments show), and each Saved Fights row gets one (the payload's captured deck — the deck entering that fight). The stock screen is hosted in `NModalContainer` (the capstone container it normally lives in is run-scoped/null at the menu) over a throwaway display-only Player; `DojoDeckViewClosePatch` redirects `NCardsViewScreen.OnReturnButtonPressed` to `NModalContainer.Clear()` for Dojo-hosted instances ONLY (stock in-run behavior untouched) — full design notes in `DojoDeckViewer.cs` and the out-of-repo work log `../VIEW_DECK_PROGRESS.md`. Search now also matches deck card names and raw `CARD.*` ids on both tabs: `DojoRunSummary.DeckCardIds` (end-of-run deck ids, **run-index disk cache schema bumped 2→3**), `DojoDisplayNames.Card`, a deck loop in `DojoRunListQueries.MatchesSearch`, and card names folded into the Saved Fights haystack. Run History search placeholder is now "Search character, fight, relic, or card" (Saved Fights placeholder deliberately unchanged — already long).
- `STS2Dojo.json` `has_pck` set to **false** (code-only mod, no Godot `.pck` yet — flip back to `true` + do a Godot export once real scenes/assets exist).

**Dev workflow on this Mac:**
- **Build:** Rider → Build (⌘F9), **or** `/usr/local/share/dotnet/dotnet build STS2Dojo.csproj` from `STS2Dojo/` (dotnet SDK 10.0.301 is installed but not on PATH — invoke by full path; needs network access for NuGet restore on first run). Either way the csproj `CopyToModsFolderOnBuild` target auto-copies dll/pdb/json into the game's `mods/STS2Dojo/`. Heads-up: the build also rewrites `STS2Dojo.json`'s BaseLib `min_version` to the *installed* version as a side-effect (currently 3.3.4); the committed manifest deliberately keeps a 3.3.2 floor, so `git checkout -- STS2Dojo.json` to drop that one-line churn after building.
- **Unit tests:** from `STS2Dojo/`, run `/usr/local/share/dotnet/dotnet run --project STS2Dojo.Tests/STS2Dojo.Tests.csproj`. The test project targets `net10.0` because this Mac has the .NET 10 runtime installed; the mod itself remains `net9.0`. The tests intentionally do **not** reference/load `sts2.dll` because loading the real game/Godot assembly outside the game process either hits architecture mismatch or segfaults. Instead they compile the production reconstruction/decision helper source against minimal DTO test doubles.
- **Run/test:** launch the game **via Steam** (Play button). **Do NOT** double-click / `open` the `.app` directly — direct launch crashes in Sentry on this Mac. The Rider ".NET Executable" launch config is a **dead end here (no Mono installed)**; for a debugger use Rider **Attach to Process** on the Steam-launched game (the mod's `.pdb` is deployed).
- **Console:** open the in-game dev console. `dojo <encounter_id>` (encounter ids autocomplete via the built-in `fight ` + Tab) for ad-hoc fights; `dojoreplay <absolute_run_file_path> <global_floor>` to launch a reconstructed fight from a real `.run` file; `dojoagain` to relaunch the last `dojo`/`dojoreplay` launch with a fresh combat RNG ("try again") (note: full command must be re-run after every rebuild — relaunch the game via Steam to pick up a new dll).
- **Logs:** `~/Library/Application Support/SlayTheSpire2/logs/godot.log`. **Save dir** (persistence checks): `~/Library/Application Support/SlayTheSpire2/`; modded runs live under `steam/<id>/modded/profile1/`.
- **Versions:** game **v0.107.1**; **BaseLib v3.3.4** installed via Steam Workshop (item `3737335127`) — the committed `STS2Dojo.json` keeps a 3.3.2 `min_version` floor deliberately (see the Build note above about the manifest-rewrite side-effect).

**Next up (durable roadmap in §9):** the §12 repeatable-seed fight sharing loop is **done and in-game validated** (export + import + same-seed determinism; §12i/§12j), and the Saved Fights UI fast-follows shipped (§12j). Remaining §12 leftovers: making modded Sentry fully silent (`TODO(telemetry)` in `SentryMacOsHangPatch.cs`), and — if ever wanted — upgrading the saved-fight row to the full read-only Replay-Setup-style stateful preview (§12h; the row is a rich always-loaded summary today, not the modal editor).
 the "Dojo — Replay Setup" modal (§5n, branch `dojo-replay-setup`, merged to `main` via PR #12) is **fully done** — build-verified, unit-tested, and now in-game smoke-tested and PASSED (2026-07-04). The full in-game smoke test of the custom Dojo run-browser screen + in-row combat view (§5i/§5j) **PASSED on 2026-07-03 — the interaction flow AND the §5h regression check both confirmed in-game (see §5l).** **§9 item 5 error-state polish** (per-category "N hidden" explanations, greyed-pill/floor "why" tooltips) is **deprioritized** — low-value UI polish, revisit opportunistically rather than as the next thing to build. **The top open item is now §9 item 6's remaining leftovers: optional repeatable seed, and making modded Sentry fully silent** (the loadout-editor half of item 6 shipped as §5n). **The `<null>`-tooltip loose end is RESOLVED at the root (2026-07-03, branch `fix/native-null-tooltips`, commit `71bb932`) — root cause found and the entire mitigation layer deleted; see §5m.** The popups were manufactured by a broken C# script-dispatch bridge on mod classes deriving directly from Godot built-ins; `DojoRunRow` is now a plain class owning a script-less `PanelContainer` and `DojoCompletionScreen` derives from the inert game class `NTopBarPortrait`. First in-game check (2026-07-03): run browser clean; completion screen still showed edge-hover `<null>` (the `NTopBarPortrait` rebase did not heal dispatch — theory falsified, see §5m's second round). Second round (root-`Ignore` + script-less-`Stop`-CenterContainer so the tooltip walk can never reach the screen node) **PASSED in-game 2026-07-03 — no `<null>` anywhere, buttons work**. The `_Ready` discriminator experiment FAILED (62 residual dispatch exceptions, harmless log noise — see §5m's verdict); the empty override was removed. `fix/native-null-tooltips` was merged to `main` via PR #11. §5h's incident is fully resolved (fixed and recovered) — but re-read its §6 rule before any future work near `UserDataPathProvider`/`SaveManager`.

---

## 1. What we're building

**STS2 Dojo** — a Slay the Spire 2 mod that lets a player replay a specific past fight from their run history, as a standalone practice combat.

Core loop: on the Run History screen, the floor-icon grid (above relics) shows each floor of a finished run. Clicking a **combat** floor opens a confirm dialog ("Replay this fight in the Dojo?"). On yes, the player is dropped into that single fight with a *reconstructed* loadout (deck, relics, HP, gold, etc.). Win or lose, the run does **not** progress — control returns to a **Completion screen** (Try Again / Return to Dojo / Return to Main Menu; see §9 item 2b) rather than back into the original run. It's a throwaway sandbox.

> **Entry-point correction (modded reality, confirmed 2026-06-30):** the game **hides Compendium → Run History when modded** (`NMainMenu` sets the Compendium button `Visible = IsCompendiumAvailable()` = false when modded), and the modded profile has its own *empty* history. So the Dojo does **not** piggyback on the game's menu path. Instead: the mod **adds its own "Dojo" button to the main menu**, and it **re-uses the game's Run History screen (`NRunHistory`) fed with the *real* profile's on-disk `.run` files** (see §9 step 5 for the how). The click → confirm → launch flow above is unchanged; only how you *reach* the run list changed.

**Environment:** macOS. Stack is the standard StS2 modding toolchain — Rider, .NET SDK, Godot/Megadot, BaseLib, the `Alchyr/ModTemplate-StS2` template. All cross-platform; nothing here is Windows-only. Watch for Mac path/`DOTNET_ROOT` quirks in `Directory.Build.props` (see template wiki Setup page).

---

## 2. Scope

**In scope (v1):**
- Single-player runs only.
- Retroactive replay — works on already-existing `.run` history files, including runs played before the mod was installed.
- Combats only (normal / elite / boss, including event-combats).
- The fight only — no surrounding floor context (no card rewards, shops, events around it).
- Fresh combat RNG each attempt (see §5 — exact RNG is impossible, not just deferred).
- A "try again" loop that re-instantiates the same reconstructed loadout without bouncing to history.

**Out of scope (v1):**
- Multiplayer (detect and exclude; see §6).
- Per-fight stats / analytics in the Dojo (possible later).
- Cross-mod content (runs that used other mods' cards/relics) — disallow if cheap; see §6.
- Deterministic / exact-original RNG replay — **impossible from the save**, do not attempt.
- Any persistence: no save writes, no rewards granted, no run progression.

**Fast-follow (post-v1, design now so we don't rewrite):**
- An editor letting the player set the remaining "unknown" fields (relic counters/props) before a replay.
- Optional user-supplied seed so attempts are *repeatable across tries* (a usability feature — NOT fidelity to the original fight).
- ~~Potion-inventory reconstruction (recoverable but lossy; see §5).~~ Implemented 2026-07-01 — see §5d.

---

## 3. Key decisions captured

| Decision | Choice | Why |
|---|---|---|
| Reconstruction approach | **Approach A** — reconstruct from the `.run` file | Simpler, works retroactively, demo-able now |
| Retroactive vs forward-only | **Retroactive** | Works on existing history; forward-only snapshotting deferred |
| Combat RNG | **Fresh per attempt** | Exact RNG unrecoverable from save; fresh is arguably better for practice anyway |
| Replay loop | **Try again** re-instantiates same loadout | Dojo wants fast retries |
| Eligible nodes | **Combats only**, detected by encounter not node type | See §6 — node type is misleading |
| Visual affordance | **None** | The icons already indicate fight type |
| Inaccuracy | **Silent inaccuracy acceptable**, but gate provably-incompatible fights as ineligible | See §6 |
| Loadout model | **Editable object with per-field provenance tags** from day one | Lets fast-follow editor sit on top without a rewrite |

There is an alternative "Approach B" (forward-only: hook combat-start during live play and serialize a *true* snapshot incl. RNG state) that yields *exact* replays but nothing retroactive. Decision: **not now.** Keep the snapshot loader behind an interface so B can be added later as a high-fidelity path that the Dojo prefers when a B-snapshot exists, falling back to A-reconstruction otherwise.

---

## 4. The data model reality (critical context)

The `.run` files are JSON (`schema_version` 8 and 9 in the wild; `build_id` ~v0.98–v0.107). **A `.run` file is an event/metrics log plus ONE end-of-run state snapshot. It is NOT a per-floor game-state snapshot.** There is no stored "state at the start of fight on floor N" — it must be *reconstructed*.

Structure:
- Top level: `acts`, `ascension`, `build_id`, `game_mode`, `killed_by_*`, `map_point_history`, `modifiers`, `platform_type`, `players`, `run_time`, `schema_version`, `seed`, `start_time`, `was_abandoned`, `win`.
- `map_point_history` is `[act][floor]`. Each floor = `{ map_point_type, player_stats[], rooms[] }`.
  - `rooms[]` carries `room_type`, `turns_taken` (always), `model_id` (the **encounter id** — note: the key is `model_id`, NOT `id`), `monster_ids` (combats only).
  - `player_stats[]` is per-player per-floor deltas: `current_hp`, `current_gold`, `max_hp`, `damage_taken`, `hp_healed`, `gold_*`, and conditional deck-delta keys (see §5).
- `players[0]` is the **end-of-run** snapshot: `character`, `id`, `deck`, `relics`, `potions`, `max_potion_slot_count`, `badges`. Deck cards carry `id`, `floor_added_to_deck` (global 1-based floor index), `current_upgrade_level`, `enchantment`. Relics carry `id`, `floor_added_to_deck`, and (final-only) stateful `props`.

A 585-file corpus was analyzed; full schema is in `SCHEMA.md` (see §7). Key empirical facts that drive design:

- `floor_added_to_deck` is a **global 1-based floor index** (48 floors = full 3-act run), aligning cleanly with the floor-N boundary.
- Two schema generations differ in load-bearing ways: `badges` is v9-only; relic counter props are under `props.card_arrays` in v8 but `props.cards` in v9. **Branch on `schema_version`.**
- 54/585 files were multiplayer.

---

## 5. Field-by-field recoverability (the reconstruction contract)

What the reconstructor can produce for "state entering fight on floor N," and how good each field is:

| Field | Recoverable? | How / caveat |
|---|---|---|
| **Encounter + enemies** | **Exact** | `rooms[].model_id` + `monster_ids`. Instantiate enemies fresh from game content tables keyed by these + top-level `ascension`. |
| **HP / gold entering fight** | **Exact (derived)** | Previous floor's `current_hp`/`current_gold`; cross-checks vs `damage_taken`/`hp_healed`. |
| **Deck** | **Exact (forward-replay, lossless)** | Start from character's true ascension-adjusted starting deck, walk floors **1→N-1** (NOT N — see §5a) applying `cards_gained` (authoritative; ignore `card_choices`/`bought_colorless`, see §5a), `cards_removed` (instance-identifiable), `cards_transformed` (`original_card`→`final_card`), `upgraded_cards`, `cards_enchanted`. Final `players[0].deck` is a cross-check (exact match once you're walking through the run's last combat floor with no further rewards). **Do NOT filter the final deck by floor** — that loses later-removed cards and shows final upgrade levels. |
| **Card upgrade timing** | **Sufficient** | `upgraded_cards` logs bare id strings (can't tell which duplicate copy upgraded), but deck is a multiset for combat purposes, so "2 Strike, 1 Strike+" is all that's needed. The duplicate-disambiguation gap is a non-issue here. |
| **Relics (which ones)** | **Exact** | Starting relics (from the true starting inventory, not `CharacterModel` alone) + `relic_choices`(picked) for floors 1→N-1 (authoritative; ignore `bought_relics`, see §5a), minus `relics_removed`. |
| **Relic counter/prop state as-of floor N** | **NOT recoverable** | `props` exist only in the end-of-run snapshot. Default to fresh/zero; expose in fast-follow editor. |
| **Potions held entering fight** | **Recoverable (implemented 2026-07-01, see §5d)** | Replay `potion_choices`(was_picked) − `potion_used` − `potion_discarded` up to floor N (`bought_potions` confirmed redundant, same as `bought_relics`). Event outcomes (Drowning Beacon, Potion Courier, Ranwid the Elder, Stone of All Time) were investigated as a possible extra source but need no special handling — every potion-touching option of theirs already goes through the same structural logging (§5d has the full story, including a wrong turn where an event-based fallback was built, merged, and then deleted). One residual gap: Potion Courier's Ransack option grants a single RNG-selected potion, unrecoverable like combat RNG generally. |
| **Combat RNG / enemy intents / enemy HP rolls** | **NOT recoverable** | Only `model_id`+`monster_ids`+`turns_taken` stored. No per-combat seed or rng counter anywhere. → fresh RNG per attempt is forced. |

**Loadout object design:** model the reconstructed loadout as an explicit, editable object where each field is tagged with provenance — `derived` (exact from log), `replayed` (forward-reconstructed), or `assumed` (defaulted because unrecoverable). The fast-follow editor is then just a UI over fields already tagged `assumed`. Baking unrecoverable fields as hard constants now would force a rewrite later.

### 5a. Reconstructor build notes (2026-06-30) — what changed vs. the plan above, and what broke

The reconstructor (`STS2Dojo/STS2DojoCode/Reconstruction/`) is built and empirically validated (§0). Building and testing it in-game surfaced several things this doc didn't anticipate. Read this before touching the reconstructor or the launch sequence.

**Architecture change: reuse the game's own save types instead of hand-parsing JSON.** The original plan (§4/§9) assumed a hand-rolled parser branching on `schema_version` 8 vs 9. Turns out the decompiled game already ships exactly the types needed, in the same assembly the mod already references:
- `MegaCrit.Sts2.Core.Runs.RunHistory` is the literal C# shape of a `.run` file (top-level `win`/`seed`/`ascension`/`map_point_history`/etc. — confirmed by grepping for `JsonPropertyName("win")` in the decompiled source).
- `MegaCrit.Sts2.Core.Saves.Migrations.MigrationManager.LoadSave<RunHistory>(path)` handles schema_version migration (v8→v9, via the game's own `RunHistoryV8ToV9` migration) for free — **no manual schema branching needed.** It needs an `ISaveStore`; `Reconstruction/LocalFileSaveStore.cs` is a minimal read-only filesystem implementation (all mutating members throw — this must never be able to touch a real save/history file).
- Per-floor deltas (`cards_gained`, `cards_removed`, `cards_transformed`, `relic_choices`, etc.) deserialize straight into the game's own `SerializableCard`/`SerializableRelic`/`PlayerMapPointHistoryEntry`/`MapPointRoomHistoryEntry` types (`MegaCrit.Sts2.Core.Runs.History` / `MegaCrit.Sts2.Core.Saves.Runs`). The reconstructor's working representation IS `SerializableCard`/`SerializableRelic` (wrapped with a `Provenance` tag), so its output can be handed straight to `CardModel.FromSerializable`/`RelicModel.FromSerializable` — the exact same path the game uses to load a saved deck.

This means `SCHEMA.md`'s hand-authored schema is still the right *reference* doc, but it's no longer the *parsing* contract — the decompiled source is now authoritative for field shapes, and re-decompiling after a game patch will surface any drift automatically (a mismatched `JsonPropertyName` would just fail to deserialize, loudly).

**Correctness bugs found by testing against `runfiles/1779595721.run` (25-floor Silent run, died to `DECIMILLIPEDE_ELITE` on floor 25) and cross-checking against a from-scratch Python re-implementation of the algorithm:**

1. **Off-by-one on "entering the fight."** §5 originally said "walk floors 1→N." Wrong: a combat floor's own `player_stats` entry bundles *that floor's own fight rewards* (e.g. floor 12 — an elite — has non-empty `cards_gained`/`relic_choices` in the SAME floor entry as the elite fight itself). Walking through floor N inclusive hands the player the rewards for the fight they're about to (re)play. Fixed: walk **floors 1 through N-1 only**. Verified against the sample file: floor 24's `current_hp`/`current_gold` (30/70, 55 gold) is the correct "entering floor 25" snapshot, not floor 25's own (post-death) values.
2. **`card_choices`/`bought_colorless` and `bought_relics` are redundant, not additive.** Applying both `card_choices`(picked) and `cards_gained` double-counted every picked reward card (confirmed: Blade Dance, Precise Cut, Snakebite, Backflip, Footwork, Afterimage all appeared twice). Same for relics: `bought_relics` duplicated `relic_choices`(picked) entries bought at a shop (e.g. Miniature Tent, floor 21). **Scanned all 531 single-player files / 13,754 floors in `runfiles/`: zero counterexamples** — every picked `card_choices`/`bought_colorless` entry is already in `cards_gained` for the same floor, and every `bought_relics` entry is already covered by a `relic_choices`(picked) entry. Fix: `cards_gained` is the sole authoritative source for card gains; `relic_choices`(picked) is the sole authoritative source for relic gains. `card_choices`/`bought_colorless`/`bought_relics` are NOT applied by the reconstructor (still useful for future UI display of "what was offered", just not for building the deck/relic list).
3. **`CharacterModel.StartingDeck`/`StartingRelics` alone is wrong at high ascension.** Silent's run had `CARD.ASCENDERS_BANE` in the final deck with `floor_added_to_deck: 1` and no corresponding `cards_gained` entry anywhere in the file — it's part of the *true* ascension-adjusted starting deck (ascension curses etc.), which only exists once the game actually creates a `Player` for that character+ascension via `Player.CreateForNewRun`. Seeding the reconstructor from `character.StartingDeck`/`StartingRelics`/`StartingHp`/`StartingGold` directly missed this. Fixed: `RunReconstructor.Reconstruct` now takes the starting deck/relics/HP/gold as **parameters**, supplied by the caller from a live throwaway `Player` (`DojoReplayConsoleCmd` snapshots `player.Deck`/`player.Relics`/`player.Creature.MaxHp`/`player.Gold` right after `StartThrowawayRun`, before mutating anything). Net effect: `RunReconstructor` no longer touches `ModelDb`/`CharacterModel` at all — it's back to being pure data in/data out.

**Runtime bugs found launching the reconstructed loadout into an actual fight:**

4. **Crash: `RunState.Contains` NRE the moment combat's hook system walks the deck.** Adding cards via `player.Deck.AddInternal(CardModel.FromSerializable(...), ...)` directly leaves `CardModel.Owner` null — `CardPile.AddInternal` doesn't set it. `Owner` is only set by `RunState.AddCard(card, owner)`, which is what `RunState.LoadCard(serializableCard, owner)` calls internally (the same method the game itself uses to load a saved deck, e.g. in `Player.SyncWithSerializedPlayer`). Fix: reconstructed cards go through `runState.LoadCard(serializableCard, player)` to get a properly-registered `CardModel`, *then* `player.Deck.AddInternal(card, ...)`.
5. **Deck counter in the top bar shows a stale count.** `NTopBarDeckButton` only refreshes on `CardPile.CardAddFinished`/`CardRemoveFinished` — NOT the per-card `CardAdded` event. `CardPile.AddInternal(card, silent: false)` never fires `CardAddFinished`; it's a separate method (`CardPile.InvokeCardAddFinished()`) the caller is expected to invoke once after a batch (this is the same pattern the game's own card-fly VFX code uses). Fix: add reconstructed cards with `silent: true` (skip per-card VFX/events), then call `player.Deck.InvokeCardAddFinished()` once after the loop.
6. **Relic icons render invisible — RESOLVED 2026-07-01, see §5c.** Relics/potions don't have a "Finished" batching hook like cards do (`NRelicInventory` subscribes directly to `Player.RelicObtained`/`RelicRemoved`), so those go through non-silent `AddRelicInternal`/`DiscardPotionInternal` calls instead. That fixes the relic *data* (confirmed: relics proc correctly — e.g. Tough Bandages triggered on a discard — and hover tooltips work), but the icon itself renders at alpha=0. Root cause: `NRelicInventory.Add(relic, startsShown: false, ...)` is the path taken for anything obtained via the `RelicObtained` event *after* `NRelicInventory.Initialize()` has already run (which is what happens for every relic reward during normal play — the icon starts invisible and is faded in by an "acquired" animation, `AnimateRelic`, that the normal reward-flow code calls explicitly). We never call that animation. The only relic that shows correctly is whichever one was already in `player.Relics` at scene-creation time (i.e. the starting relic) — `NRelicInventory.Initialize()` adds those with `startsShown: true`. **Fix requires mutating `player.Relics`/`player.Deck` *before* `NRun`'s scene is created**, i.e. before `NRelicInventory.Initialize()` runs — which means bypassing the convenience wrapper `NGame.StartNewSingleplayerRun` (which creates the scene internally) for the raw 3-step §8.0 sequence (`RunState.CreateForNewRun` → `RunManager.Instance.SetUpNewSingleplayer(shouldSave:false)` → mutate `runState.Players[0]` → `RootSceneContainer.SetCurrentScene(NRun.Create(runState))`), with `DojoLaunch` exposing a hook/callback for the mutation step. This turned out to be real restructuring, not a one-line fix, and it overlapped with §9 roadmap item 2 ("intercept combat-end") as predicted — both were done together. §5c has what actually shipped.

### 5b. RunReconstructor unit-test harness (2026-07-01)

`STS2Dojo/STS2Dojo.Tests/` is a lightweight console test project for agentic development. It deliberately avoids xUnit/NUnit/NuGet packages and deliberately does **not** load the real `sts2.dll` at runtime. Loading the installed game assembly outside the game/Godot process failed on this Mac (x86_64 game assembly vs arm64 dotnet, and an arm64 assembly load path segfaulted), so the harness instead compiles the production reconstruction/decision helper source directly against minimal DTO test doubles in `STS2Dojo.Tests/TestDoubles/`.

Run it from `STS2Dojo/`:

```
/usr/local/share/dotnet/dotnet run --project STS2Dojo.Tests/STS2Dojo.Tests.csproj
```

Current passing fixtures:
- `1779595721.run` floor 25 — known empirically validated Silent A10 elite (`DECIMILLIPEDE_ELITE`), 24-card deck, 10 relics, HP 30/70, gold 55.
- `1781906039.run` floor 2 — recent v0.107.1 first combat, checks previous-floor HP/gold and ascension-adjusted starting deck.
- `1782434199.run` floor 9 — target-floor card/relic rewards are excluded (off-by-one guard).
- `1782705511.run` floor 5 — event-combat case: `map_point_type: unknown`, but room is a real monster combat.
- `1782524433.run` floor 48 — recent full-length Ironclad A10 boss win.
- `1782182181.run` floor 48 — late Silent A10 boss with prior relic removals.
- `1781843292.run` floor 50 — mutation-heavy late boss stress fixture.
- `1782696823.run` floor 49 — single-player Defect A10 final boss. Note: `1782788638.run` looked like a useful Defect A10 final-boss fixture during corpus search, but it is multiplayer and belongs in a future negative/gating test instead.

Current passing decision-helper test groups:
- **Combat floor detection:** `monster`/`elite`/`boss` rooms count as combat; non-combat rooms are rejected; event-combat fixture `1782705511.run` floor 5 is found by room type despite `map_point_type: unknown`.
- **Single-player gate:** accepts `players.Count == 1 && players[0].Id == 1`; rejects multiplayer fixture `1782788638.run`; `RunReconstructor` now fails fast with a clearer single-player error.
- **Eligibility/resolve pass:** `DojoContentEligibility` collects missing encounter, monster, card, and relic ids from a reconstructed loadout through an `IDojoContentResolver` seam.
- **Run history file selection/filtering:** classifies non-`.run` files, load failures, unsupported schema versions, multiplayer runs, modifier runs, no-combat runs, and includable runs.
- **Real-profile path construction:** builds `user://steam/<id>/profileN/saves/history` and strips the `modded/` segment from a modded profile base path.

Potion reconstruction acceptance suite (part of the same `dotnet run` command above, no opt-in flag — see §5d for the implementation, including a bug the last 3 fixtures below now guard against):
- `1773537304.run` floor 5 — Drowning Beacon's Bottle option leaves Vulnerable Potion + Glowwater Potion entering the next combat (structurally tracked).
- `1773538354.run` floor 17 — `RELIC.PETRIFIED_TOAD` leaves `POTION.POTION_SHAPED_ROCK` in inventory alongside earlier potions.
- `1777731795.run` floor 28 — Potion-Shaped Rock discarded by The Future of Potions is gone entering the next elite.
- `1776407214.run` floor 21 — Potion Courier's Grab Potions option leaves two Foul Potions entering the next combat (structurally tracked).
- `1778868022.run` floor 31 — Foul Potions used at a merchant are removed, and the shop-bought potion isn't double-counted.
- `1782178743.run` floor 14 — regression fixture: a Drowning Beacon event where the **Climb** option was chosen (gains a relic, not a potion) must not leak a Glowwater Potion grant. Also asserts `MaxPotionSlots` reflects Ascension 10's Tight Belt reduction (2, not the base 3).
- `1777590946.run` floor 21 — regression fixture: a Stone of All Time event where the **Push** option was chosen must not remove the held Skill Potion. Also asserts `MaxPotionSlots` reflects a Potion Belt-style relic bonus (6).
- `1779159270.run` floor 25 — regression fixture: Potion Courier's **Ransack** option offered a potion with no open slots (2/2 full), so nothing should be guessed into the inventory; Potion Belt is picked up one floor later, asserted via `MaxPotionSlots`.

Corpus scan: all 216 `bought_potions` entries were already represented by a picked `potion_choices` entry on the same floor (redundant, like `bought_relics`) — confirmed, not just assumed.

Because the test project uses DTO doubles, it is best for the pure reconstruction algorithm and schema-shape regressions. It is **not** an integration test for `RunHistoryLoader`, `MigrationManager`, `DojoLaunch`, Godot scene creation, live `ModelDb` content resolution, relic-icon rendering, save suppression, or combat-end interception.

### 5c. Relic-visibility fix + combat-end interception (2026-07-01) — resolves §5a point 6 and §9 roadmap item 2

Branch `fix/relic-visibility-combat-end` (commits `0c58ec0`, `c74b58b`). Research this round went directly against `decompiled/` rather than trusting this doc's earlier static analysis, which undersold the real scope of "intercept combat-end" (see below) — then the result was empirically confirmed in-game.

**`DojoLaunch.cs` rewritten.** `StartThrowawayRun`/`EnterEncounter` are gone; the new entry point is `DojoLaunch.LaunchThrowawayRun(game, character, ascensionLevel, encounterId, mutate)`, which manually replicates `NGame.StartRun` (the private helper `NGame.StartNewSingleplayerRun` calls internally) instead of calling that wrapper:
```
RunState.CreateForNewRun(...) → RunManager.Instance.SetUpNewSingleplayer(shouldSave:false)
→ CombatReplayWriter.IsEnabled = false → DojoRunRegistry.MarkAsDojo(runState)
→ PreloadManager.LoadRunAssets/LoadActAssets → RunManager.Instance.FinalizeStartingRelics() → RunManager.Instance.Launch()
→ mutate(runState)   // <<< the fix: deck/relics/hp/gold replaced HERE, before the scene exists
→ game.RootSceneContainer.SetCurrentScene(NRun.Create(runState))
→ RunManager.Instance.EnterRoomDebug(...)
```
Confirmed **`EnterAct(0)` (map/act entry) can be skipped entirely** — `EnterRoomDebug` only requires `RunManager.State != null` and short-circuits map/act lookups when given a concrete `EncounterModel` (exactly like the built-in `fight` console command, which never calls `EnterAct` either — no map/act state is needed for a single scripted encounter). One caveat worth remembering: `Player.AddRelicInternal` does **not** call `RelicModel.AfterObtained()` — that's the caller's job (see `RelicCmd.Obtain`/`RunManager.FinalizeStartingRelics`). `DojoReplayConsoleCmd` deliberately does **not** call it for reconstructed relics: every reconstructed relic's one-time pickup effect (e.g. `Pear`'s permanent Max HP boost, `Whetstone`'s one-time card upgrades) already happened earlier in the *original* run and is already baked into the run file's logged HP/gold/upgrade values that `RunReconstructor` reads directly — calling `AfterObtained()` again would double-apply those effects.

`DojoLaunch.TryAgain()` remembers the last launch request and replays it (fresh RNG each time, per §3); backs the new `dojoagain` console command.

**Combat-end interception (`DojoCombatEndPatches.cs`, `DojoCombatEndInterceptor.cs`).** Turned out there's no single "combat ended" hook — an ordinary win never calls `RunManager.OnEnded` at all (only a full-run "final boss" victory does); the real win hook is the `CombatManager.CombatWon` event, and loss detection is a direct synchronous call inside `CreatureCmd.Kill`, not an event. Three Harmony patches, all gated on `DojoRunRegistry.IsCurrentRunDojo()` so real player runs are untouched:
- `NCombatUi.OnCombatWon` prefix → skip (returns `false`) for Dojo runs, which skips the rewards screen + map entirely.
- `NRun.ShowGameOverScreen` prefix → skip for Dojo runs, which skips the game-over screen.
- Both route through `DojoCombatEndInterceptor`, which now shows `DojoCompletionScreen` directly on top of the still-alive combat scene via `NModalContainer`. This replaced the old direct-to-Run-History redirect (`ReturnToMainMenu()` + `PushSubmenuType<NRunHistory>()`), which was a dead end once the Dojo got its own main-menu entry point. `ReturnToMainMenu()` only runs later if the player clicks **Return to Dojo** or **Return to Main Menu**, safely outside the still-unwinding combat-end call stack.

The key design choice here: the completion UI is a modal overlay, not a scene transition. It keeps combat-end interception synchronous and minimal, then lets each button decide when to tear down the throwaway run. **Try Again** clears the modal and calls `DojoLaunch.TryAgain()`. **Return to Dojo** clears the modal, returns to main menu, then opens the custom `NDojoScreen`. **Return to Main Menu** clears the modal and returns to main menu only.

**Secondary-write suppression (`DojoCombatEndPatches.cs`).** Research surfaced the §8.0 punch-list was incomplete: `SaveManager.Instance.UpdateProgressAfterCombatWon(...)` + `SaveManager.Instance.SaveProgressFile()` are called **unconditionally on every combat win** (not just the previously-documented loss-path `CheckUpdateEnemyDiscoveryAfterLoss` leak inside `RunManager.OnEnded`). All three are now Harmony-patched off for Dojo runs. `replays/latest.mcr` needs no patch — `CombatReplayWriter.IsEnabled = false`, set once at launch, is checked by every write site. `prefs.save` was traced to only 2 call sites (`NGame.Quit()`, the settings screen) — **neither fires on combat end**; the original spike's finding was very likely just the ordinary app-quit write from the Steam-relaunch-per-build dev loop, not a Dojo-specific leak. Left unpatched; flagged for a targeted re-check if it resurfaces.

**Two robustness bugs a code review caught and fixed (same branch, commit `c74b58b`):**
- A failed `mutate` callback (e.g. `dojoreplay` hitting unresolvable content mid-reconstruction) left `RunManager.State` permanently occupied — `RunManager.SetUpNewSingleplayer` throws `"State is already set."` for *any* subsequent run, Dojo or real, until something else happened to call `CleanUp()` first. Fixed: `LaunchInternal` now wraps the mutate/asset/scene-creation sequence in a try/catch that calls `RunManager.Instance.CleanUp()` immediately on failure and rethrows.
- `DojoRunRegistry.Unmark` had no call sites — every `dojoagain` retry pinned the previous `RunState` (and its whole object graph) in the registry for the process lifetime. Fixed: a Harmony prefix on `RunManager.CleanUp()` unmarks the current state automatically, so every teardown path (the launch guard, the win/loss redirect, the new failure-recovery path above) stays correct without remembering to call `Unmark` at each call site.

**Verified in-game:** relic icons in `dojoreplay` render correctly and immediately (headline fix); win/loss both skip rewards/map/game-over; `dojoagain` retries with fresh RNG. Later UI work verified the Completion screen visually in-game and exercised its live Steam-mod build path. A full post-PR #3 smoke pass through every Completion-screen button (plus the macOS screenshot `<null>`-tooltip check) has since been done manually and passed (2026-07-02) — see §5e. Not yet separately re-confirmed: the save-leak byte-diff / `prefs.save` question from the original §8.0 plan.

A good easy-win fixture for future in-game smoke tests (win path): `dojoreplay <path>/runfiles/1780077009.run 45` — Silent A10, single-monster `ENCOUNTER.GLOBE_HEAD_NORMAL` hallway fight, 110/110 HP with zero damage taken in the original run.

### 5d. Potion reconstruction implemented (2026-07-01) — including a wrong turn worth remembering

Potions were the last field in the §5 table stuck at "Assumed" (v1 shipped empty slots, per the §10 decision at the time). Implemented now, closing the gap. **This section originally documented a design that was built, unit-tested, and merged — then found wrong via in-game testing.** Left in below because the wrong turn is the useful part: it's a concrete example of why corpus correlation alone isn't sufficient evidence, and the fix is small once you see it. Read this before touching potion logic.

**Structural replay is the whole feature.** `potion_choices`(picked) is the sole authoritative gain source; `potion_used`/`potion_discarded` are removals. `bought_potions` is confirmed redundant (a corpus scan of all 216 entries found each one already duplicated by a picked `potion_choices` entry on the same floor — zero counterexamples, same pattern already established for `bought_relics`/`card_choices`), so it's not applied.

**The wrong turn: an event-based fallback for a gap that didn't exist.** A corpus scan of `event_choices[].variables` found ~150 instances across the 531-file corpus where an event (Drowning Beacon, Potion Courier, Ranwid the Elder, Stone of All Time) mentions a potion by name/count with no matching `potion_choices`/`potion_used`/`potion_discarded` entry that floor. The first implementation treated this as a real recoverability gap and added event-driven fallback grants/removals, keyed on the event's *name* (`event_choices[].title.key`'s prefix before `.pages.`) — e.g. "any `DROWNING_BEACON` event with a `Potion` variable grants that potion." This passed 8 unit tests (including hand-corrected fixtures) and got merged. **It was wrong**, caught only when a live in-game `dojoreplay` run showed 3 potions at Ascension 10 with a 2-slot cap.

The actual bug: `event_choices[].variables` reflects **every one of an event's possible options**, populated once at generation time so each option's hover text/description has something to show — regardless of which option the player actually picks. `event_choices[].title.key` in the log is the option *chosen*, but the `variables` dict doesn't change based on that choice. So matching on event name alone fires for every option of that event, not just the potion-touching one. Checking the decompiled event models (`Core/Models/Events/{DrowningBeacon,PotionCourier,RanwidTheElder,StoneOfAllTime}.cs`) directly — rather than trusting the corpus correlation — showed:
- `DrowningBeacon`: only the **Bottle** option grants Glowwater Potion (via the normal `RewardsCmd.OfferCustom`/`PotionCmd.TryToProcure` reward flow — always structurally tracked, 6/6 in corpus). **Climb** grants a relic (Fresnel Lens) and costs HP — no potion touched at all, yet its `Potion` variable is populated identically to Bottle's.
- `RanwidTheElder`: only the **Potion** option removes a potion (via `PotionCmd.Discard`, which unconditionally logs to `potion_discarded` — always tracked, 9/9). **Gold** and **Relic** options never touch potions.
- `StoneOfAllTime`: only the **Lift** option discards a potion (again via `PotionCmd.Discard` — always tracked, 11/11). **Push** doesn't touch potions.
- `PotionCourier`: **Grab Potions** offers exactly `FoulPotions`-count Foul Potions via the normal reward flow (always tracked, 11/11). **Ransack** is the one genuine exception — it grants a **single random uncommon-rarity potion** chosen via `base.Owner.PlayerRng`, i.e. real RNG, not a fixed "3 Foul Potions" (the `FoulPotions` DynamicVar is shared flavor text, not Ransack's actual output). This is unrecoverable the same way combat RNG is (§5) — Ransack is mostly still structurally tracked when the reward screen logs the decline/pick (24/28 in corpus), and the few residual cases are correctly left as a silent gap rather than guessed.

Net result: **every genuine potion-touching option across all four events is already 100% structurally tracked.** There is no event-only gap to bridge. The entire fallback (event-name matching, the `IPotionNameResolver`/`KnownPotionNames` display-name table, the reflection bridge for reading the game's boxed `DynamicVar`/`StringVar` types, the test-double `EventOptionHistoryEntry`/`LocString` plumbing) was deleted. Three of the acceptance fixtures were rebuilt as regression tests instead — same run files, but asserting that the *non-potion* option (Climb/Push/no-room-Ransack) correctly leaves potions untouched, which is exactly the scenario the deleted code got wrong.

**The one real addition beyond structural replay: max potion slot tracking.** This is what actually caused the in-game symptom (3 potions, 2 slots) and is a genuine gap structural replay alone doesn't cover — nothing in `map_point_history` records slot count directly. `RunReconstructor` now tracks it explicitly per floor:
- Base 3 slots (`Player.initialMaxPotionSlotCount`), minus 1 for the whole run if `ascension >= 4` (`AscensionLevel.TightBelt`, applied via `AscensionManager.ApplyEffectsTo` — confirmed this already happens automatically for the Dojo's throwaway player too, since it's called from `RunManager.SetUpNewSingleplayer`'s `InitializeNewRun`, which `DojoLaunch` does call).
- Plus a one-time bonus wherever `relic_choices`(picked) includes `RELIC.POTION_BELT` (+2), `RELIC.ALCHEMICAL_COFFER` (+4), or `RELIC.PHIAL_HOLSTER` (+1) — confirmed via each relic's `AfterObtained()` in the decompiled source, which is the complete set (grepped `AddToMaxPotionCount`/`SubtractFromMaxPotionCount` across all of `decompiled/`). These relics' own `AfterObtained()` also grants potions via `PotionCmd.TryToProcure`, so those are already structurally tracked separately — only the *slot* bonus needed separate tracking, since `DojoReplayConsoleCmd` deliberately never calls `AfterObtained()` for reconstructed relics (§5c: their one-time effects are already baked into the run's logged values) and a slot count has no logged value to bake into.

Exposed as `ReconstructedLoadout.MaxPotionSlots`. `DojoReplayConsoleCmd`'s mutate callback reconciles the throwaway player's slot count to exactly this value (grow *or* shrink via `AddToMaxPotionCount`/`SubtractFromMaxPotionCount`) rather than the original wrong approach of growing slots to fit however many potions got reconstructed — that was the actual mechanism of the in-game bug: it silently expanded capacity to paper over a data problem instead of surfacing it.

**Current fixture set (8, all always-run, see §5b):** the original 5 structural fixtures unchanged, plus 3 regression fixtures (`1782178743.run` floor 14, `1777590946.run` floor 21, `1779159270.run` floor 25) that specifically assert the non-potion-option case stays inert, each also asserting `MaxPotionSlots` to lock in the ascension/relic-bonus computation.

**Re-confirmed in-game (2026-07-02):** re-running the same `dojoreplay` command that originally surfaced the 3-potions/2-slots bug now shows the correct reconciled slot count.

### 5e. Dojo entry point, real-profile run browser, and Completion screen (2026-07-02)

Merged to `main` in PR #3 (`644f67f`, feature branch `feature/dojo-entry-point-and-completion-screen`). This resolves §9 item 2b and the first pass of §9 item 5: there is now a player-facing path from main menu → real-profile Run History → combat floor → reconstructed Dojo replay → Completion screen.

**Main-menu entry point (`DojoMainMenuPatch.cs`).** There is no appendable runtime button list on `NMainMenu`: `MainMenuButtons` is computed over hardcoded scene-baked fields. The shipped approach is to Harmony-postfix `NMainMenu._Ready`, duplicate the existing `SettingsButton` (`NMainMenuTextButton`) without signal duplication, reown unique-name references via `DojoNodeDuplication.ReownRecursively`, relabel it to "Dojo", add it to `MainMenuTextButtons`, and move it to the Settings slot so it appears between Multiplayer/Timeline and Settings. Because `NMainMenu.ConnectMainMenuTextButtonFocusLogic()` has already run by then, the patch reflects into the private `MainMenuButtonFocused`/`MainMenuButtonUnfocused` methods so the new button uses the same gold hover-reticle animation as the built-in buttons.

**Real-profile run browser (`DojoRunBrowser.cs`).** > **OBSOLETE (superseded by §5i then §5j).** This describes the very first pass, where the Dojo reused the game's `NRunHistory` screen by temporarily flipping `UserDataPathProvider.IsRunningModded = false`, with restore hooks on submenu-close and scene-swap. That whole approach is gone: §5i replaced the landing screen with the custom `NDojoScreen` (real profile read directly, no flip), and §5j replaced the last remnant (the "View All Combats" drill-in) with the in-row floor map. `DojoRunBrowser.cs` today is only the unconditional `DojoRunHistorySaveSafetyPatch`. Kept here for the historical record of why the flip existed and why it was dangerous (§5h). Original text:
- The Dojo reused the game's `NRunHistory` screen instead of building a custom browser; `NRunHistory`/`SaveManager` resolve the run-history path from the current value of `UserDataPathProvider.IsRunningModded`, so `DojoRunBrowser.Open` temporarily set that flag to `false` while the screen was open, with two restore paths (submenu-close postfix; `NSceneContainer.SetCurrentScene` prefix for the launch path).

The important safety invariant at the time: the flag must never stay `false` after a launch, or the modded session could accidentally read/write the real profile. (Now moot — nothing flips it.)

**Floor click → replay (`DojoRunHistoryFloorClickPatch.cs`, `DojoReplayLauncher.cs`).** `NMapPointHistoryEntry.Create` is patched to attach a click handler to replayable floors only: single-player, no modifiers, and an actual combat room. The confirmation dialog uses the base generic popup node but writes raw text directly into `NVerticalPopup` because the mod has no localization table. Confirming launches through `DojoReplayLauncher`, a shared path now used by both UI and `dojoreplay`: resolve encounter, snapshot true ascension-adjusted starting deck/relics/HP/gold from the throwaway player, run `RunReconstructor`, replace deck/relics/potions/HP/gold, reconcile potion slots, and enter the fight.

**Completion screen (`DojoCompletionScreen.cs`).** Code-only modal shown by `DojoCombatEndInterceptor` after win/loss. It deliberately avoids a `.pck`/Godot scene for now:
- Reuses the real rewards-screen chrome/title style by instantiating `screens/rewards_screen` off-tree, extracting the `Rewards` panel and `%HeaderLabel` style, stripping unwanted children, then rebuilding contents inside it.
- Uses custom `DojoCompletionEventOptionButton : NButton` for three consistent-width buttons. The button is an event-option-style drawn polygon with a light brown/gold three-band gradient; the label style is duplicated from the game's generic popup yes-button so the font matches the rest of the UI.
- Buttons are: **Try Again** (`DojoLaunch.TryAgain()`), **Return to Dojo** (`ReturnToMainMenu()` then `NDojoScreen.Open(game)`), and **Return to Main Menu** (`ReturnToMainMenu()`).
- Hover/flicker fix: buttons own their own visual state and use the full fixed button bounds as the hit area; the earlier attempt based on duplicated popup/proceed buttons shared material/visual assumptions that caused all buttons to highlight or flicker.
- `<null>` tooltip mitigation: the modal clears existing `NHoverTipSet`s, returns an empty Godot native tooltip string, hides the game hover-tip container while open, and sets/restores `NHoverTipSet.shouldBlockHoverTips`. This was added because macOS screenshot-capture mode could let an underlying hover tip appear on top of the modal.

**Audio cleanup:** `DojoLaunch` now stops both `NRun.Instance?.RunMusicController` and `NAudioManager.Instance` music before launching/retrying. This matches the base game's run-start behavior and avoids a lingering end-of-combat sting or menu track overlapping the next Dojo fight.

**Validation done:** repeated live builds to the Steam mods folder with `/usr/local/share/dotnet/dotnet build STS2Dojo.csproj`; temp builds with `-p:ModsPath=/private/tmp/sts2mods/`; in-game screenshot/visual iteration on the completion screen (font, centering, button width, hover behavior, button style/color, and tooltip behavior). A clean smoke pass after a fresh Steam launch through all three Completion-screen buttons plus the macOS screenshot shortcut (to confirm the final `<null>` mitigation) has since been done manually and passed (2026-07-02).

### 5f. Live eligibility/resolve pass wired into replay launches (2026-07-02) — closes §9 roadmap item 4

The pure eligibility logic (`DojoContentEligibility.Validate`, the `IDojoContentResolver` seam) was already built and unit-tested against `FakeContentResolver` (§5b) but had no live implementation and wasn't called anywhere — `dojo`/`dojoreplay` just let a missing id surface as a raw, unhelpful exception wherever `ModelDb.GetById` happened to be called deep inside card/relic loading. Closed now:

- **`Reconstruction/LiveDojoContentResolver.cs`** — a live, `ModelDb`-backed `IDojoContentResolver`. `CanResolve` dispatches on `DojoContentKind` to `ModelDb.GetByIdOrNull<T>(id) != null` for the matching model type (`EncounterModel`/`MonsterModel`/`CardModel`/`RelicModel`/`PotionModel`). `ModelDb`'s internal `_contentById` dictionary is keyed by `ModelId` across every content type at once (category is baked into the id), so this is a plain dictionary lookup, not a scan. Deliberately **not** added to `STS2Dojo.Tests`'s explicit `<Compile Include>` list — it depends on real `sts2.dll` types the test harness never loads (§5b); the pure `DojoContentEligibility` logic stays covered there via `FakeContentResolver`.
- **`DojoContentEligibilityException`** (in `DojoContentEligibility.cs`, so it stays test-harness-compatible) — carries the missing-content list and a human-readable message (`"Dojo replay is ineligible - missing content: Card:CARD.X, Relic:RELIC.Y"`).
- **`DojoReplayLauncher.LaunchReplay`** now calls `DojoContentEligibility.Validate(loadout, LiveDojoContentResolver.Instance)` immediately after `RunReconstructor.Reconstruct(...)` returns, and **before** any of the player-mutation calls that follow (`Deck.Clear`, relic/potion removal, etc.). A miss throws `DojoContentEligibilityException` from inside the `mutate` callback passed to `DojoLaunch.LaunchThrowawayRun` — this lands squarely in the failure path `DojoLaunch.cs` already had in place (mutate/asset/scene-creation wrapped in try/catch → `RunManager.Instance.CleanUp()` → rethrow; see §5c), so an ineligible fight aborts cleanly with no scene ever created and no `RunManager.State` left occupied, with no new plumbing needed there. `LaunchReplay`'s own catch block logs `DojoContentEligibilityException` by message only (expected/user-facing refusal, not a bug worth a stack trace), falling through to the existing generic `catch (Exception e)` for everything else.
- This covers both halves of the §6 eligibility rule for free: a fight referencing content removed/renamed since the source run was played (e.g. a game-patch drift case), and a fight that used an uninstalled mod's content — both surface as the same clear refusal instead of a crash.
- Unit-tested: `ContentEligibilityResolvePass` in `STS2Dojo.Tests/Program.cs` now also asserts `DojoContentEligibilityException.Message` names the missing content, still using `FakeContentResolver` (the pure-logic side is exactly what's testable outside the game process).

**Deliberately not done here** (at the time — closed by §5g below): no UI-facing error state yet for an ineligible floor click — a refusal from the browser still just logged and silently no-opped from the player's perspective, same as any other launch failure. `DojoConsoleCmd` (the `dojo <id>` junk-loadout spike command) is untouched — it doesn't go through `RunReconstructor`, so there's no reconstructed loadout to validate beyond the encounter id it already resolves synchronously.

### 5g. Ineligible floors greyed out, eligible floors pre-highlighted, in the run browser (2026-07-02) — partial §9 item 5 follow-up

> **Partly superseded by §5j (2026-07-03):** this was written for the (now removed) stock-`NRunHistory` drill-in. Two things changed when the in-row floor map took over the only remaining consumer of `NMapPointHistoryEntry.Create`: (1) the grey-out now applies **only to ineligible COMBAT floors** — non-combat floors (rest/shop/event/treasure) are left rendered normally, matching the mock (a bare non-combat floor was never a replay target, so greying it was noise); (2) the **pre-highlight (`DojoFloorHighlightPatches`) and its `EligibleEntries`/`IsMarkedEligible` tracking were removed** — the mock wants uniform icon sizes, and enlarging eligible floors risked overlap in the dense in-row strip. The `IsEligible` grey-out + click-wiring below are unchanged; the paragraph on the pre-highlight and the "eligible floors shown pre-highlighted" framing no longer apply.

§5f made a floor click *fail* cleanly when its content doesn't resolve, but the player only found out after clicking — every floor icon looked identical regardless of eligibility. Fixed: ineligible floors are now visibly disabled before a click happens, and eligible floors are shown in their "picked" look by default instead of only on hover.

- **`DojoFloorEligibility.cs`** (new) — `IsStructurallyReplayable` (the pre-existing single-player/no-modifiers/combat-room checks, extracted unchanged from `DojoFloorClickPatch` so there's one source of truth) plus `IsEligible`, which adds a live content-resolve pass. The hard part: content eligibility needs the *true* ascension-adjusted starting deck/relics (§5a point 3's "Ascender's Bane" problem), which so far had only ever been obtained from a live throwaway player created via the *full* `DojoLaunch` sequence (`RunManager.SetUpNewSingleplayer`, asset preload, scene creation). That's far too expensive to run per floor icon (up to 48 per run, every time the browser opens a run). The fix: skip `RunManager` entirely. `RunState.CreateForNewRun(...)` sets `player.RunState` directly inside its own `CreateShared` step — no `RunManager.SetUpNewSingleplayer` call needed for that — so `Player.CreateForNewRun(character, UnlockState.all, netId) → RunState.CreateForNewRun([player], ...) → new AscensionManager(ascension).ApplyEffectsTo(player)` reproduces the same true starting inventory `DojoReplayLauncher` snapshots, entirely in memory, with no asset loading, no scene, and nothing ever registered with `RunManager` — the whole throwaway object graph is just garbage-collected when the function returns. From there it's the same `RunReconstructor.Reconstruct` + `DojoContentEligibility.Validate(loadout, LiveDojoContentResolver.Instance)` pipeline `DojoReplayLauncher` already uses. Any exception (bad data, unresolvable content, a reconstruction error) is caught and treated as "not eligible" — same refuse-don't-crash philosophy as `DojoContentEligibilityException`.
- **`DojoFloorClickPatch.Postfix`** now calls `DojoFloorEligibility.IsEligible` instead of re-implementing the structural checks inline. An ineligible floor gets `Modulate = StsColors.disabledTopBarButton` (the same grey/dim tint the base game already uses for a disabled top-bar button — reused for visual consistency rather than inventing a new color) and is left unwired (no `Released` handler). Deliberately does **not** call `NClickableControl.Disable()`: that would also kill the floor's pre-existing, unrelated hover-stats tooltip (`NMapPointHistoryEntry.OnFocus`), which the player may still want even for a floor they can't replay.
- Confirmed (by grepping `decompiled/` for callers) that nothing in the Run History screen ever calls `NMapPointHistoryEntry.AnimateIn` — so there's no reveal-animation tween that would later overwrite the grey tint back to full white; `_Ready()` (which runs after `Create()`, once the node enters the tree) only forces `Modulate.A` back to `1`, leaving the R/G/B tint from the postfix untouched.
- **Eligible floors are additionally pre-highlighted, not just left at default appearance.** `NMapPointHistoryEntry.Highlight()`/`Unhighlight()` are the existing hover grow/shrink methods (large icon + bright outline vs. base size); previously they only ran on mouse-over. Two more small patches (`DojoFloorHighlightPatches`, same file): a `_Ready()` postfix calls `Highlight()` once for eligible floors right after the node's texture/outline node references exist (they're null until `_Ready()` runs, so this can't happen from `Create()`'s postfix — the field values needed don't exist yet); an `Unhighlight()` prefix returns `false` (skips the original) for eligible floors, so losing focus never shrinks them back down. `OnFocus`/`OnUnfocus` and their tooltip logic are untouched — `Highlight()` still runs on hover for an eligible floor, but it's now just a no-op tween (already at the target scale/color), so in practice hovering an eligible floor's only visible effect is the tooltip. A `ConditionalWeakTable<NMapPointHistoryEntry, object>` (not a plain set — must not keep a Godot node alive past the run browser closing) tracks which entries `DojoFloorClickPatch.Postfix` already marked eligible, shared via `DojoFloorClickPatch.IsMarkedEligible`.
- **Not yet done:** no tooltip/explanation for *why* a floor is greyed out (a future hover-tip addition would help — e.g. "this floor's content is no longer available"); not yet exercised in-game (build-verified only, same caveat as any change made this session).

### 5h. Real-profile corruption incident: `IsRunningModded` left `false` across a save (2026-07-02) — CRITICAL, read before touching `DojoRunBrowser.cs`

**A real player-reported incident, not a hypothetical.** The user reported that after disabling mods in-game and restarting into the non-modded game, the Compendium (Run History) button had vanished from the main menu — on their **real** profile, unrelated to anything Dojo-specific from the player's perspective. Root cause traced entirely from decompiled source, no reproduction needed to diagnose:

- `SaveManager.IsCompendiumAvailable()` is literally `Progress.NumberOfRuns > 0` (or `!IsReleaseGame()` if zero), and `ProgressState.NumberOfRuns => Wins + Losses` — real, cumulative stats read from `progress.save`.
- `DojoRunBrowser.Open()` (§5e) flips the single global static `UserDataPathProvider.IsRunningModded = false` for as long as the Dojo's real-profile Run History screen is open, so `SaveManager`/`NRunHistory` (which resolve every profile-scoped path live from this flag) read the real profile's `.run` files instead of the modded one. It was restored via two hooks: backing out of the screen (`NSubmenu.OnSubmenuClosed`), or launching a replay (`NSceneContainer.SetCurrentScene`, since a launch tears down the scene directly without popping the submenu stack).
- **The gap:** neither hook fires if the game quits/restarts, or if the player reaches some *other* screen that saves on its own close, while the Dojo browser is still open underneath — e.g. `NGame.Quit()` (window close, and whatever "restart to apply mod changes" does) unconditionally calls `SaveProgressFile()`/`SaveProfile()`/`SavePrefsFile()`, and separately `NSettingsScreen.OnSubmenuClosed` unconditionally calls `SavePrefsFile()` on every close, independent of quitting.
- **Why this corrupts the real profile, not just writes to the wrong place harmlessly:** each of those three save methods resolves its *file path* live from `UserDataPathProvider.IsRunningModded` — but always serializes the *current process's in-memory* `SaveManager.Instance.Progress`/profile/prefs state, which was loaded from the **modded** profile when the (modded) game process launched. If the flag was still `false` at the moment of the save, the modded session's own (near-empty, low/zero `Wins+Losses`) progress data gets written to the **real** profile's `progress.save`, overwriting genuine stats. This is a silent, no-crash data-loss bug — nothing about it would look like an error at the time.
- **Fix (`DojoRunBrowser.cs`):** rather than trying to enumerate every screen/flow that might independently trigger a save while the Dojo browser happens to be open (an open-ended, easy-to-miss set), added `DojoRunHistorySaveSafetyPatch` — a Harmony prefix on the three actual chokepoint methods themselves, `SaveManager.SaveProgressFile`/`SaveProfile`/`SavePrefsFile`, unconditionally forcing `UserDataPathProvider.IsRunningModded = true` immediately before any of them runs. Deliberately unconditional (doesn't check `DojoRunBrowser.ConsumeRestorePending()` first, unlike the other two hooks) — in a modded session the flag should *always* be true by the time anything saves, full stop, regardless of whether the Dojo browser was ever opened that session or exactly how it was left. This can only ever prevent a modded session from writing to the real profile; it can never cause the reverse, so there's no correctness downside to it firing "unnecessarily."
- **Recovery — RESOLVED (2026-07-02).** No clean rollback existed: the local `progress.save.backup` had already rotated to the same corrupted content, and Steam Cloud (both the local sync-cache mirror and, per `remotecache.vdf`'s `remotetime`, the server copy) had already synced the corrupted version too, before the user thought to check. Time Machine wasn't configured on this Mac either (`tmutil listlocalsnapshots` / `tmutil destinationinfo` both empty). What actually recovered it: a one-off Python script (session-scratchpad only, not committed anywhere in this repo — see the note below) that rebuilt `progress.save`'s derivable fields by replaying all 591 intact single-player `.run` history files in chronological (`start_time`) order, reimplementing the relevant parts of `ProgressSaveManager.UpdateWithRunData`/`UpdateAfterCombatWon` from decompiled source: per-character `total_wins`/`total_losses`/`current_streak`/`best_win_streak`/`fastest_win_time`/`max_ascension` (including the "ascension only bumps if you win *at* your current max" nuance), `card_stats`, `encounter_stats`/`enemy_stats` win/loss per character, and the `discovered_cards`/`relics`/`potions`/`events`/`acts` sets. Fields the `.run` log genuinely can't reconstruct (`architect_damage`, `epochs`, `unlocked_achievements`, `total_unlocks`, `wongo_points`, `test_subject_kills`, `ancient_stats`, multiplayer ascension) were left at the corrupted file's existing values rather than guessed — a disclosed gap, not a silent one. Result: 86 wins / 447 losses across 5 characters (`NumberOfRuns = 533`), confirmed in-game (Compendium back) after write.
- **A second wrinkle during recovery, itself worth remembering:** writing the fixed file to disk didn't stick on the first two tries. First, the game was still running with the *old* corrupted state loaded in memory — quitting normally flushed that stale in-memory copy back over the fix (same underlying mechanism as the bug itself: whatever's in memory wins on save, regardless of what's freshly on disk). Second, even with the game closed, **Steam Cloud sync re-downloaded the still-corrupted server copy on the next launch and clobbered the local fix again** — cloud sync applied unconditionally on launch here, not "newer wins." The fix only held once the user disabled Steam's per-game "Keep game's saves in the Steam Cloud" setting (Library → right-click → Properties), at which point the local write survived a relaunch; re-enabling Cloud Saves afterward and relaunching did then push the good version up without reverting again. **Lesson: any local file fix to a game-managed save must account for the game process's own in-memory state AND any active cloud-sync layer — writing to disk once is not sufficient confirmation of a fix.**
- **Note for future sessions:** the reconstruction script only ever existed in that session's ephemeral scratchpad — it was never added to this repo. If a similar reconstruction is ever needed again (or you want to generalize it into a real recovery tool), it has to be rebuilt from scratch; this bullet plus the method description above is what a future session has to go on.

### 5i. Custom Dojo run-browser screen (2026-07-02) — replaces the stock-NRunHistory landing screen

Branch `feature/dojo-custom-run-browser`. The Dojo main-menu button (and the Completion screen's "Return to Dojo") now open `NDojoScreen` (`DojoScreen.cs`), a code-built full-screen run browser modeled on a user-provided mock: left sidebar (title, search box, character-icon / ascension / victory filter chips, reset, back), sort row (Newest/Oldest/Floor/Ascension), and a scrolling list of run rows (character avatar token + A-badge, VICTORY/DEFEAT/ABANDONED, max floor, end HP, date/time, duration, deck/relic counts, seed, death quote via the game's own `NRunHistory.GetDeathQuote`, and a per-act chronological fight list of room-icon pills with border-based win/loss state). Each row now also **expands in place to the full per-act floor map — see §5j.** **Build-verified + unit-tested; the landing screen renders in-game (§5k) and the full interaction smoke test + §5h regression check both passed in-game 2026-07-03 (§5l).** Key decisions and hard-won facts:

- **The run list never touches `UserDataPathProvider.IsRunningModded`.** `DojoRunIndex` computes the real profile's history dir by stripping `/modded/` from the current profile base path (`RealProfilePath.BuildHistoryPathFromProfileBasePath` + `ProjectSettings.GlobalizePath`, resolved on the main thread) and reads `.run` files directly via `RunHistoryLoader`/`LocalFileSaveStore` (read-only by construction). ~~The §5h-guarded flag flip now exists ONLY in the "View All Combats" drill-in (`DojoRunBrowser.OpenAtRun`)~~ **— superseded by §5j (2026-07-03): the drill-in and its flag flip are GONE (replaced by the in-row floor map), so the Dojo no longer flips `IsRunningModded` anywhere at all.**
- **Pushing a custom code-only submenu works.** `NSubmenuStack.Push(NSubmenu)` is public; `NDojoScreen : NSubmenu` is added as a child of the stack and pushed. Two base-class landmines: `NSubmenu._Ready()` throws if a subclass calls it (subclasses override `_Ready` → own `ConnectSignals()`), and base `ConnectSignals()` requires a scene-baked `"BackButton"` child — so the screen overrides it WITHOUT calling base and replicates the visibility→shown/hidden wiring itself.
- **ESC/back hotkey shadowing (real bug caught in self-review):** `NHotkeyManager` invokes only the **last-pushed** binding per hotkey (`LastOrDefault()`); stock submenus keep exactly one live cancel binding by Disabling their back button whenever the submenu is hidden (Disable → `UnregisterHotkeys`). Any custom control registering cancel/back hotkeys MUST replicate that enable-on-show/disable-on-hide dance or it silently eats ESC everywhere once its screen has been opened (the screen stays in the tree, hidden, after Pop).
- **Code-created labels need an explicit theme font override.** `MegaLabel._Ready` throws `InvalidOperationException` if `HasThemeFontOverride("font")` is false ("Godot engine bug" guard) — so `new MegaLabel()` without an override crashes on entering the tree (which also means `DojoCompletionScreen`'s plain-fallback path would have crashed if ever exercised — it always used templates in practice). `DojoUi` extracts the game font once from a generic-popup label template and stamps it (plus size/color overrides) onto plain Godot `Label`s instead.
- **Per-pill eligibility gating uses the same `DojoFloorEligibility` pipeline as the floor grey-out (§5g)**, made cheap two ways: `DojoFloorEligibility` now caches the throwaway starting snapshot per (character, ascension) — building the preview `Player`/`RunState` was the expensive part, the replay itself is list-walking — and `NDojoScreen` memoizes per-pill verdicts in a `ConditionalWeakTable` keyed by summary (invalidates naturally when `DojoRunIndex` re-summarizes a changed file; bounded by the live run set).
- **Memory: summaries must not pin parsed runs.** `DojoRunSummary.RunSource` is a re-load factory + `WeakReference` cache instead of a strong `RunHistory` reference — the session-lifetime index cache for a ~1000-run profile would otherwise pin tens-to-hundreds of MB of parsed per-floor history. Rows load the run on demand (weakly cached, seeded from the index-build parse) and degrade gracefully ("Run file could not be read.") if the file vanished since indexing.
- **Unreached bosses are shown as a dimmed "Boss not reached" placeholder, NOT a specific boss name** (the mock showed a dimmed real name, e.g. "Knowledge Demon"). The `.run` file only logs *visited* floors — which boss the unreached act would have held is not recoverable without regenerating the map from the seed. Deliberate scope cut; revisit only if seed→map regeneration is ever built.
- Sidebar character list comes from `ModelDb.AllCharacters` (all 5, incl. Regent/Necrobinder) — no hardcoded roster; the filters and row avatars use the same `CharacterModel.IconTexture` art path as the stock run-history/top-panel UI, while display names/colors still come from `CharacterModel.Title`/`NameColor` via `DojoDisplayNames` (memoized, prettified-id fallback for unresolvable content). Death quotes strip `[bbcode]` tags since rows render plain labels.
- Search is debounced (0.18s one-shot `Godot.Timer`); rows are built in incremental batches of 12 as the scroll approaches the bottom (no virtualization needed at ~1000 runs).
- **Excluded runs surface as a count, not rows:** the status line shows "N hidden (multiplayer, modifiers, or unsupported)" from `RunHistoryFileSelector` decisions — the §9 item 5 error-state polish (per-category explanations, greyed-pill tooltips) is still open.

### 5j. In-row combat view + full removal of the `IsRunningModded` flip (2026-07-03) — build-verified, unit-tested, and smoke-tested (§5l)

The per-row **"View All Combats" drill-in** (which pushed the stock `NRunHistory` over the Dojo screen, and was the mod's **last** `UserDataPathProvider.IsRunningModded = false` flip — the exact §5h/§6 corruption mechanism) is replaced by an **in-place expand**. `DojoRunRow` (`DojoScreen.cs`) now toggles between its collapsed summary and the full per-act floor map, so the whole flag-flip apparatus could be deleted. Two goals, both met: build the in-row view, and eliminate the flip.

- **The floor map reuses the stock widget — no hand-built icons or tooltip.** For each act, for each visited floor, `DojoRunRow.BuildFloorMap` instantiates a stock `NMapPointHistoryEntry` through `DojoFloorClickPatch.CreateDojoEntry(history, floorEntry, globalFloor)` (1-based global floor numbering, identical to the stock `NActHistoryEntry`/`RunHistoryQueries.FlattenFloors` scheme) and lays it in a per-act HBox. That inherits, for free: the correct room icon (`ImageHelper.GetRoomIconPath`) and the exact per-floor hover tooltip (`NMapPointHistoryHoverTip`, fired on `OnFocus`). The Dojo-only helper scopes the `DojoFloorClickPatch` postfix so only these in-row icons receive ineligible-combat grey-out and click-to-replay wiring; plain stock Run History icons are untouched.
- **`SetPlayer` is mandatory and timing-sensitive.** `NMapPointHistoryEntry.OnFocus` throws `"Player has not been set!"` if `_player` is null, and `SetPlayer` reads `%QuestIcon`, which only exists after the node's `_Ready`. So the map is built off-tree, added to the (in-tree) row, and only THEN is `SetPlayer(history.Players[0])` called on each entry — on the main thread `AddChild` runs descendants' `_Ready` synchronously (confirmed against `AddChildSafely` and the stock `NRunHistory.DisplayRun` → `LoadHistory` (adds to tree) → `SelectPlayer` → `SetPlayer` ordering).
- **`DojoFloorClickPatch` generalized to the mock's rules and scoped to Dojo-owned icons.** Previously it greyed EVERY non-eligible floor (fine for the stock screen, wrong for the in-row map, which shows all floor types). Now: non-combat floors (`!IsCombatFloor`) are left rendered normally; only an **ineligible COMBAT** floor is greyed; eligible combats get the `Released → DojoReplayConfirmation.ConfirmAndLaunch` handler. Because stock Run History can still be opened through the Compendium in some modded sessions, the Harmony postfix is opt-in: `DojoRunRow` calls `DojoFloorClickPatch.CreateDojoEntry`, which sets a scoped marker while invoking `NMapPointHistoryEntry.Create`; plain stock `Create` calls return unchanged. The §5g pre-highlight (`DojoFloorHighlightPatches` + `EligibleEntries`/`IsMarkedEligible`) was **removed** — the mock wants uniform sizes and enlarging risked overlap in the dense strip.
- **Wide acts fit without breaking the outer list.** A single act can be ~18 floors; each act's strip is wrapped in its own horizontal `ScrollContainer` (`HorizontalScrollMode.Auto`, `VerticalScrollMode.Disabled` so a vertical wheel still scrolls the outer run list), so a wide act scrolls internally instead of forcing the row wider than the viewport. The outer `ScrollContainer`'s `HorizontalScrollMode.Disabled` is untouched; toggling never resets the outer vertical scroll (no `ScrollVertical = 0` on the toggle path).
- **Don't pin `NMapPointHistoryEntry` nodes.** The expanded view is built lazily on first expand and freed on collapse (`ClearBody` → `QueueFreeSafely`), so the stock floor-icon nodes never outlive what the player has open (§5i's rule). Multiple rows can be expanded at once — each `DojoRunRow` owns its own state, no single-open constraint. The collapsed pill strip keeps its per-summary `PillEligibilityCache` (moved onto `DojoRunRow`); the expanded map's per-floor verdicts come from `DojoFloorEligibility` (snapshot-cached) at `Create` time.
- **Flag flip fully gone.** `DojoRunBrowser.cs` is now just `DojoRunHistorySaveSafetyPatch`; `Open`/`OpenAtRun`, `DojoRunHistoryFlagRestorePatch`, `DojoRunHistorySceneSwapRestorePatch`, `DojoRunHistoryTargetRunPatch`, `ConsumeRestorePending`/`ConsumeTargetRunFileName`, and the `_restorePending`/`_targetRunFileName` fields were deleted. The safety patch dropped its `ConsumeRestorePending()` call but keeps the **unconditional** `IsRunningModded = true` force (§5h/§6 — kept as a pure last line of defense even though nothing flips the flag to `false` anymore). A repo-wide grep confirms no code writes `IsRunningModded = false`; the only write is the safety patch's `= true`.
- **Smoke-tested in-game 2026-07-03 (§5l) — passed.** Remaining polish (non-blocking): hover-enlarge (1.5× on focus, the stock behavior) can clip top/bottom against the per-act scroll viewport's content-height; the greyed-floor "why" tooltip is still open (§9 item 5); the ineligible-combat ✕ overlay from the mock is not drawn (greying is the affordance).

### 5k. UI polish pass + first in-game render of the custom screen (2026-07-03) — see PRs #7 & #9

Two small visual-polish PRs on `DojoScreen.cs`, then the first time `NDojoScreen` was actually launched in-game.

- **PR #7 (`feature/dojo-browser-ui-tweaks`):** collapsed run-row toggle renamed "View Combats" → "View All Combats"; run-row avatar rings enlarged (`DojoUi.MakeCharacterToken` 58→64, radius 32, content margin 5→8, portrait still 48px) for more breathing room; the character name-color avatar ring dimmed a notch — `DojoRunRow.ResolveCharacterColor` now returns `model.NameColor.Darkened(0.18f)`, since the raw name colors (e.g. The Silent's green) read too neon as a solid ring. The dim applies to *every* character's ring (most visible on green), not just The Silent — a deliberate choice over hardcoding one character id.
- **PR #9 (`feature/dojo-character-filter-chips`):** the sidebar character-filter icon chips were a squat 46×36 pill, so the portrait grazed the edge and looked crowded. Now they're **52×52 circles with 11px uniform icon padding** (`DojoChip.Configure`: `iconOnly` width/height both 52, insets 11), and that row's **"All" pill was bumped to 52 tall** to stay aligned — the other filter rows (ascension/victory/sort) stay `compact` 36. `_Draw`'s corner radius is `Size.Y/2`, so a 52×52 chip draws as a true circle. Lesson: an earlier 2px inset-only bump (5/4→7/6) wasn't enough at 36px tall — the real fix was a bigger button, not more inset.
- **First in-game render (confirmed 2026-07-03):** the Dojo landing screen renders live — sidebar (title/search/character+ascension+victory filters) and the run list (avatars, A-badges, per-act pills) — with **no crash on open**. This is the first runtime confirmation the custom screen works at all. **Now fully verified (2026-07-03):** the interaction flow (row expand → per-act floor map → hover tooltip → click an eligible combat → confirm → replay → completion screen → collapse → multi-row expand → wide-act scroll → ESC) and the §5h regression check both passed — the §0 "top open item" / §9 item 5 smoke test is **done**. See §5l.

### 5l. Full in-game smoke test PASSED (2026-07-03) — custom screen + in-row combat view validated end-to-end

The §0 "top open item" that had blocked confidence in the whole custom-browser UI (§5i/§5j/§5k) is closed. A manual in-game pass on a Steam-launched build confirmed:

- **Interaction flow — all passed clean:** main menu → Dojo → filters / search / sort → row expand → three per-act floor-icon strips render → hover a floor for the stock `NMapPointHistoryEntry` tooltip → click an eligible combat → confirm popup → reconstructed replay launches → win/loss → Completion-screen buttons → collapse → multiple rows expand independently → wide-act horizontal scroll doesn't break the outer list → ESC behaves on and off the screen. No crashes, no visible regressions.
- **§5h regression check — passed:** with a row expanded, reached Settings, toggled a setting that saves, quit and relaunched UNMODDED, and the real profile's Compendium + stats were intact. This is the expected outcome now that nothing flips `UserDataPathProvider.IsRunningModded` anymore (§5j), but it's now *empirically* confirmed, not just reasoned from the code.
- **Scope caveat — the tested build did NOT include the uncommitted `<null>`-tooltip work.** As of this writing there is uncommitted, unbranched work sitting directly on `main`: a new `STS2DojoCode/DojoNativeTooltips.cs` plus edits to `DojoScreen.cs` and `DojoCompletionScreen.cs` that factor the Completion-screen `<null>`-tooltip mitigation into a shared helper and extend it to `NDojoScreen` (periodic `_Process` cleanup + a `_MakeCustomTooltip` override + `ProcessMode.Always`). It is **separate from and NOT validated by** this smoke test, and it's a hygiene loose end (uncommitted, on `main` rather than a branch). A future session should decide its fate — branch + build + verify the `NDojoScreen` `<null>`-tooltip behavior in-game, or discard it — and until then treat that behavior as unverified. **RESOLVED 2026-07-03: that mitigation attacked a symptom; the root cause was found and fixed on branch `fix/native-null-tooltips` and the whole mitigation layer was deleted — see §5m.**

With this pass done, the durable next items are §9 item 5's remaining error-state polish (per-category hidden-run explanations, greyed-pill/floor "why" tooltips) and the §9 item 6 fast-follow set.

### 5m. The `<null>` native-tooltip root cause: broken script dispatch on Godot-direct-derived mod classes (2026-07-03)

Branch `fix/native-null-tooltips` (commit `71bb932`). The flashing native Godot tooltips reading `<null>` on the Dojo run browser and the completion screen were never a tooltip-content problem, and none of the content-side mitigations (clearing `TooltipText`, `_GetTooltip`/`_MakeCustomTooltip` overrides, reactive popup removal, raising `gui/timers/tooltip_delay_sec` at runtime) could ever have fixed them. The actual mechanism, established from the game logs plus the Godot 4.5 engine source (the game runs Megadot `4.5.1.m.12.mono.custom_build`):

- **In this modded game, every C# bridge call into a mod script class that derives DIRECTLY from a GodotSharp built-in (`Control`, `PanelContainer`, `ScrollContainer`, …) throws a native CoreCLR `ArgumentException`**, logged as `System.ArgumentException: Undefined resource string ID:0x80070057` (the message text lives in `libcoreclr.dylib` — it is CoreCLR failing to resolve its own exception-message resource; 0x80070057 = E_INVALIDARG). Mod classes that derive from a *game* class (`NButton`, `NSubmenu`, `NTopBarPortrait`, …) dispatch fine — every working Dojo widget (chips, pills, the screen itself) proves this in-game daily. The logs from 2026-07-03 show ~22k such exceptions per session for exactly the three Godot-direct classes that existed (`DojoRunRow`, `DojoScrollContainer`, `DojoCompletionScreen`) and zero for the game-derived ones; the first throw fires during a plain `AddChild` (layout `_get_minimum_size` dispatch), so even the UNMATCHED dispatch path throws — no override or lack thereof changes anything.
- **Godot converts that swallowed exception into the literal text `<null>`.** `CSharpInstanceBridge.Call` catches the managed exception, returns a NIL variant, and — upstream bug — leaves `Callable::CallError` at its default `CALL_OK`. The `GDVIRTUAL_CALL` macro therefore treats the call as having succeeded and casts NIL to `String`, which stringifies as `"<null>"`. `Viewport::_gui_get_tooltip` walks up from the hovered control calling `get_tooltip()` on each (mouse-PASS containers let it reach the row/screen), receives `"<null>"`, and dutifully shows a native tooltip saying `<null>`. The "flash" was the mouse crossing between throwing and non-throwing controls plus the old reactive cleaner freeing the popup after the fact.
- Consequences worth remembering: the broken classes' `_Ready`/`_Process`/`_ExitTree`/`_Draw` overrides **never executed at all** (the completion screen worked only because buttons are game-derived and everything else was direct C# calls), and `ProjectSettings.SetSetting("gui/timers/tooltip_delay_sec", …)` at runtime is a no-op because each `Viewport` caches the delay at construction, long before any mod loads. `Viewport.cancel_tooltip()` exists in the 4.5 C++ source but is not exposed to scripting in Megadot.

**The fix (all mitigation deleted, ~360 lines net removed):** `DojoRunRow` is no longer a Node subclass — it is a plain C# class owning a script-less `PanelContainer` (`Root`), which `NDojoScreen` adds to the list (no script instance ⇒ no dispatch ⇒ nothing to throw ⇒ `get_tooltip` uses the native empty `tooltip_text`). **Verified in-game 2026-07-03: the run browser's `<null>` popups are gone.**

**Falsification + second round (same day):** `DojoCompletionScreen` must remain a node (`NModalContainer.Add` hard-casts its argument to `IScreenContext`), so the first attempt rebased it onto `NTopBarPortrait` — an inert game `Control` subclass — on the theory that a game base class heals dispatch. **In-game test falsified that:** the completion screen still threw (626 exceptions in the session log, the first during `BuildLayout`'s `AddChild`) and still showed `<null>` when hovering the modal's edge/backdrop area (the only region whose tooltip walk ascends all the way to the screen root — the central panel area breaks the walk earlier, which is why the popup only appeared near the edges). So "derives from a game class" is NOT the discriminator. Re-fitting all nine data points, the surviving pattern: every working mod class declares (or inherits from a mod class that declares) a recognized lifecycle override (`_Ready` etc.); every broken one contributed none below the Godot built-ins. The second-round fix is two-layered: (1) **guaranteed, engine-native:** the screen root's mouse filter is now `Ignore` and the full-rect `CenterContainer` is the script-less `MOUSE_FILTER_STOP` control — `Viewport::_gui_get_tooltip` calls `get_tooltip` on every ascended node *including* the STOP one that terminates the walk, so the only reliable defense is ensuring no hover chain can ascend into the script node at all; the CenterContainer both swallows modal input (the root's old job) and terminates every walk first; (2) **experimental:** an empty `_Ready` override to test the lifecycle-override discriminator. **Verdict (in-game, 2026-07-03): the experiment FAILED and the filter structure is the protection.** The session log after the second-round deploy showed zero `<null>` popups (user-confirmed clean) but still 62 `DojoCompletionScreen` dispatch exceptions (50 `InvokeGodotClassMethod` + 12 `HasGodotClassMethod` — lifecycle/layout dispatch at screen build, the hover-walk ones gone). So the lifecycle-override theory is falsified too; the empty `_Ready` was removed again. **The true discriminator for which mod script classes get the broken bridge remains unknown** (falsified: bare-Godot-vs-game base class; declaring lifecycle overrides) — parked as unknowable without Megadot internals or a debugger. Practical consequences: a §5m-broken class leaks ~60 harmless log-noise exceptions per completion-screen visit (bounded, build-time only), and any future mod node class should get the layer-(1) treatment (or better, the `DojoRunRow` non-node treatment) from day one. `DojoNativeTooltips.cs` and every `_GetTooltip`/`_MakeCustomTooltip` override are gone, and the character filter chips no longer set `TooltipText`, so the mod has zero native-tooltip surface (the game itself never uses native tooltips anywhere — its 1.9 GB pck contains exactly two `tooltip_text` entries, both empty strings).

**Open questions deliberately left alone:** the exact CoreCLR-level reason the Godot-direct-derived dispatch throws (it is infrastructure-level, hitting even `HasGodotClassMethod`'s pure StringName comparisons; plausibly a Megadot-vs-stock-`Godot.NET.Sdk` interaction, since mods compile against the stock NuGet GodotSharp but run against MegaCrit's custom build) — parked as unknowable without Megadot source or a debugger. `Godot.Bridge.ScriptManagerBridge.LookupScriptsInAssembly(...)` (the template wiki's "Things to Note" line, still commented out in `MainFile.Initialize`) was considered and deliberately NOT enabled: every currently-working class works without it, both throwing and working classes were equally unregistered (so it cannot be the discriminator), and enabling it would have muddied attribution of this fix. If a future mod class ever NEEDS to derive directly from a Godot built-in, try that line first — and expect §5m symptoms if it does not help.

### 5n. "Dojo — Replay Setup" modal: pre-launch relic/card state editor (2026-07-03) — merged to `main` via PR #12, in-game smoke test PASSED (2026-07-04)

Branch `dojo-replay-setup`. Replaces the one-line "Replay this fight? Yes/No" popup (`DojoReplayConfirmation.cs`, deleted) with a single-screen setup modal (`DojoReplaySetupModal.cs`), realizing the §9 item 6 "loadout editor over `assumed` fields" fast-follow for relic counter/prop state and stateful cards. Both entry points (custom-screen fight pills, in-row floor-map click) now open `NDojoReplaySetupModal.Open(history, floor)`.

**What the player sees (per the user-provided mock):** gold serif "Dojo" title + thin gold rule; a read-only summary strip (encounter name, "Floor N · Character · Ascension N", HP/Gold/Potions stat chips); an italic explainer line; a RELIC STATE section with one row per stateful relic in the loadout (circular relic-sprite icon, name, helper text, − value + stepper, Zero/Random/Primed presets — a preset shows gold-filled when the current value equals what it sets, Random never does); a CARD STATE section rendered only when the deck holds ≥1 stateful card (rounded-square card-portrait icon, stepper only, one row per COPY of a stateful card); the stock red/green ribbon Cancel/Start buttons. The list scrolls internally; title/summary/actions stay fixed. Start is the default-focused control, so confirm-hotkey = instant launch with defaults; ESC = Cancel (both hotkeys come with the reused `NPopupYesNoButton`s).

**How adjusted state reaches the fight — the whole feature is one insight:** relic/card counters live in `[SavedProperty]` properties, and the game restores them via `SavedProperties.Fill` whenever a `SerializableRelic`/`SerializableCard` carries a `Props` blob through `RelicModel/CardModel.FromSerializable` — the exact pipeline `DojoReplayLauncher`'s mutate callback already feeds. So `DojoStateAdjustments.ApplyTo(loadout)` (called right after the eligibility gate, before any player mutation) just stamps `Props` onto the reconstructed DTOs and the values restore exactly like a mid-run save reload. No new relic/card mutation code at all.

- **Registry (`DojoStatefulContent.cs`):** every `[SavedProperty]`-bearing relic (40) and card (4) in v0.107.1 was reviewed (grep for the attribute over `decompiled/`). 29 relics + 3 cards got editable specs (threshold counters like Pen Nib/Nunchaku/Happy Flower, charge counters like Pumpkin Candle/Ember Tea, bools like Lizard Tail/Venerable Tea Set rendered as Yes/No steppers). Deliberately excluded, with reasons in the class doc: map/run-structure state (FurCoat coords, GoldenCompass, SpoilsMap), record-keeping for content already reconstructed from the log (ArchaicTooth/DustyTome/TouchOfOrobas/PaelsTooth/SeaGlass), cosmetics (skins), auto-reset state (LavaLamp), and secondary props (SilverCrucible.TreasureRoomsEntered, Wongo's GaveRelic). Matched by model TYPE (`model switch { PenNib _ => ... }`), so a game-patch rename breaks the build loudly. Thresholds/primed values read from each model's decompiled source. Linked-property state (Genetic Algorithm's CurrentBlock+IncreasedBlock, The Scythe's CurrentDamage+IncreasedDamage) writes both props from the one displayed value. Two private `[SavedProperty]`s (TeaOfDiscourtesy.CombatsLeft, SilkenTress.IsUsed) use string names — `SavedProperties.Fill` resolves NonPublic.
- **Row defaults = exactly what an unadjusted launch produces** (the reconstructor deliberately drops per-floor props as unrecoverable, §5); `BuildAdjustments` skips rows left at default, so "just hit Start" is byte-identical to the pre-modal behavior.
- **Summary strip data comes from a preview reconstruction** via the new `DojoFloorEligibility.TryReconstructPreview` (same cached starting-snapshot machinery as the §5g eligibility checks — microseconds after first use). ⚠️ The preview loadout's `Derived` entries ALIAS the shared snapshot cache; it must be treated READ-ONLY (documented on the method). Adjustments are only ever stamped on the launch-time loadout, whose DTOs are rebuilt fresh per launch inside the mutate callback.
- **Card rows key by (card id, occurrence index in deck order)** — stable between preview and launch because both replay the same log over the same (character, ascension) starting inventory and the reconstructor replaces deck entries in place. Each copy of a stateful card gets its own row (named "Genetic Algorithm+ (2)" etc.).
- **Chrome reuse:** stone panel = rewards screen "Rewards" frame (same extraction as `DojoCompletionScreen`); title = duplicate of the generic popup's gold serif Header; Cancel/Start = duplicates of the popup's `NPopupYesNoButton`s (per the task: keep the stock red/green ribbon shapes). Each duplicated button's Image/Outline **materials are made unique** — the focus tint tweens shader params on the material, and a shared material instance tints every user at once (the §5e completion-screen flicker bug, prevented rather than re-fixed). `IsYes` is set AFTER `NModalContainer.Add` (its setter re-registers hotkeys and reads %-resolved internals, which need `_Ready` to have run); `DojoNodeDuplication.ReownRecursively` applied to every duplicate per its class doc. The explainer's italics use the game's real `italics_font` theme font (resolved from the popup Description's RichTextLabel theme), with a `FontVariation` shear fallback.
- **§5m/§6 compliance:** the modal derives from `NTopBarPortrait` + `IScreenContext` (`NModalContainer.Add` hard-casts), relies on NO lifecycle overrides on itself (built + wired imperatively from `Open`), root mouse filter `Ignore` with the script-less full-rect `CenterContainer` as the `Stop` control terminating every native-tooltip walk. New widgets (`DojoStepperChip`, `DojoPresetChip`) derive from `NButton` like the known-working `DojoChip`.
- **Validated:** builds clean; all 32 unit-test groups pass (the registry itself isn't unit-testable — it references game types the §5b harness never loads, same as `LiveDojoContentResolver`). **In-game smoke test PASSED (2026-07-04):** modal opens from both entry points, stone panel renders at the computed size, ribbon buttons look/behave right (hover tint isolated per button), steppers/presets update + show correct gold active states, Start launches with adjusted state applied, ESC cancels and returns cleanly, a deck with multiple copies of a stateful card shows one row per copy.

---

## 6. Cross-cutting rules

**Anything touching `UserDataPathProvider.IsRunningModded`, or any other profile-scoped save path, is safety-critical — a bug here can corrupt the player's REAL, non-modded save, not just the throwaway modded/Dojo one.** §5h is the concrete case study: a temporarily-flipped global flag plus an unrelated code path saving on its own (`NGame.Quit()`, a Settings screen closing) silently overwrote the real profile's `progress.save` with modded-session data, and it took real effort to recover (see §5h's recovery bullet). The durable lesson, not just the specific fix: whenever new code reads or writes anything under a profile-scoped path (`UserDataPathProvider.GetProfileDir`/`GetProfileScopedPath`, or any `SaveManager` save/load method), ask **"what happens if a save fires while this is in a state I didn't anticipate?"** before assuming the known call sites are the only ones that matter. Prefer a hard, unconditional safety net at the actual read/write chokepoint (as `DojoRunHistorySaveSafetyPatch` now does on `SaveManager`'s save methods themselves) over patching every caller you can currently think of — new callers, including ones added by a future game patch, won't respect a safety net that only lives at today's call sites. Treat any PR touching this area with the same scrutiny you'd give code that directly deletes save files.

**Mod C# classes that live in the scene tree must derive from a GAME class, never directly from a Godot built-in.** §5m is the case study: `DojoRunRow : PanelContainer` and `DojoCompletionScreen : Control` had a broken script-dispatch bridge — every engine call into them threw, their `_Ready`/`_Process`/`_Draw` never ran, and the engine turned the swallowed exceptions into literal `<null>` native tooltips on hover. Prefer not being a Node at all (plain class owning script-less Godot nodes, like `DojoRunRow` now). If a class MUST be a node (interface casts like `NModalContainer.Add`'s `IScreenContext`), know that deriving from an inert game class (`NTopBarPortrait`) was verified NOT to fix dispatch — the working mod classes all derive from substantial game classes (`NButton`, `NSubmenu`) AND declare lifecycle overrides; and regardless, keep the node out of every native-tooltip walk (script node's filter `Ignore` + a script-less full-rect `Stop` child that terminates the walk — see `DojoCompletionScreen.BuildLayout`). If you see `System.ArgumentException: Undefined resource string ID:0x80070057` spam in `godot.log`, this is what it means.

**Eligibility gate = one resolve-pass (covers both version drift AND cross-mod).** At reconstruction time, collect every content id the fight references — `model_id`, each `monster_id`, every reconstructed card id, every relic id — and check each resolves against the *currently-loaded* content registry (base game + active mods). Any miss → fight is ineligible. This gives the "Doormaker doesn't exist on v107" case and the "run used an uninstalled mod" case for free, with no version-comparison table. Build this first; it doubles as the reconstruction validator.

**Combat detection keys off the room, not the node.** A floor is replayable iff its `rooms[]` has a combat `room_type` (`monster`/`elite`/`boss`), equivalently has `monster_ids`. **Do not filter by `map_point_type`** — a node can be `map_point_type: "unknown"` (an event) yet contain a real fight (event-combat). The sample run's floor 24 (`THE_OBSCURA`) is exactly this: `unknown` node, real combat inside. Filtering by node type would both miss event-combats and risk offering replay on non-combat nodes.

**Multiplayer exclusion = two-field check at file load.** Single-player iff `len(players) == 1 and players[0].id == 1`. MP runs use 17-digit Steam IDs. Gate at load so MP runs never render replay icons.

**Schema branching is mandatory.** Branch on `schema_version` (8 vs 9) in the loader; relic-prop location and `badges` differ. Refuse gracefully ("run saved on unsupported format") on any version outside the validated set rather than guessing.

---

## 7. Artifacts that already exist

- `runfiles/SCHEMA.md` — full empirical schema from a 585-file corpus, including a "Q&A" section answering 11 targeted questions (removals, transforms, upgrade timing, potions, RNG, combat detail, relic props, multiplayer, schema drift, structural invariants, id space) with file-level evidence, plus a "still unknown / need more samples" list. **Read this before writing the reconstructor.** (Present in `./runfiles/`.)
- A sample `.run` file (`runfiles/1779595721.run`) — Silent, ascension 10, v0.106.1, a complete losing run (died to `DECIMILLIPEDE_ELITE`). Good first reconstruction target.
- ~585 sample runs in the `./runfiles/` directory.
- `decompiled/sts2/` — full decompiled game source (the spike's evidence base). `sts2.dll` is the assembly it came from; re-decompile after a game patch to refresh.
- `STS2Dojo/` — scaffolded mod project (Godot + .NET, ModTemplate layout, git-initialized).
- `STS2Dojo/STS2Dojo.Tests/` — no-NuGet console test harness for `RunReconstructor` and pure decision helpers; see §5b.

> The `schema_probe.py` script mentioned in earlier handoffs is **not needed** and is not in this workspace — `SCHEMA.md` already captures the corpus analysis. If a future game patch is suspected of changing the format, re-derive from the run files directly rather than maintaining the probe.

---

## 8. The sandbox spike

### 8.0 SPIKE VERDICT (static pass complete — 2026-06-30)

**Approach A is validated — statically AND empirically (2026-06-30).** Static analysis of `decompiled/` put all three questions green; the empirical rung-3 confirmation has now been run on the Mac via a `dojo <encounter_id>` dev-console command (`STS2Dojo/STS2DojoCode/DojoConsoleCmd.cs`) that runs the §8.0 launch sequence with `shouldSave:false`.

**Empirical rung-3 result (save-dir byte-diff across win + abandon + death):** ✅ **ZERO run-history entries created** on any end-state (real history 1070→1070, modded history 0→0), no resumable run save written, real profile untouched. So `shouldSave:false` correctly gates `CreateRunHistoryEntry` and the run-save path — even through a full rewards→map→"save and continue" flow and the death/`OnEnded(false)` game-over path. **Punch-list — 3 secondary writes leak (all inside the throwaway `modded/profile1`, none touching run history or the real save), NOT gated by `shouldSave`:** `progress.save` (progression/discoveries — note `CheckUpdateEnemyDiscoveryAfterLoss` sits *before* the `if(ShouldSave)` block in `OnEnded`), `replays/latest.mcr` (CombatReplayWriter), `prefs.save` (modded-profile prefs). The real Dojo must suppress the progression grant and the replay writer. Also confirmed as a design requirement: the Dojo **must intercept combat-end** to skip rewards/map (now done, §5c) — though the original plan to return straight to Run History is itself superseded (§9 item 2b).
>
> **This punch-list is superseded — see §5c for the current picture.** It undercounted: `progress.save` also leaks unconditionally on the WIN path (`SaveManager.UpdateProgressAfterCombatWon`/`SaveProgressFile`), not just the loss-path `CheckUpdateEnemyDiscoveryAfterLoss` noted here. All of `progress.save` (both paths) and `replays/latest.mcr` are now Harmony-patched/suppressed for Dojo runs. `prefs.save` was traced to NOT actually be caused by combat end at all (only `NGame.Quit()`/the settings screen write it) — likely a false positive from the dev workflow, left unpatched.

The game already contains every primitive the Dojo needs, and they compose into this launch sequence (the same one `NSceneBootstrapper.StartNewRun()` and `NGame.StartNewSingleplayerRun()` use):

```
RunState.CreateForNewRun(players, acts, modifiers, GameMode.Standard, ascension, seed)
RunManager.Instance.SetUpNewSingleplayer(runState, shouldSave: false)   // ← save kill-switch
RootSceneContainer.SetCurrentScene(NRun.Create(runState))               // ← scene swap
// mutate runState.Players[0]: deck / relics / hp / gold / potions  (= the reconstructor output)
RunManager.Instance.EnterRoomDebug(encounter.RoomType, MapPointType.Unassigned, encounterModel)  // ← launch fight
```

> **⚠️ Superseded ordering — do not copy this block.** This spike-era sketch mutates the player *after* `SetCurrentScene`, which is exactly the ordering that caused the relic-icon-invisibility bug (§5a point 6). The real shipped sequence (§5c) mutates *before* `SetCurrentScene`, calls it via `game.RootSceneContainer` (an instance property — `RootSceneContainer` isn't a static class, as this sketch implies), and includes steps this sketch omits entirely (asset preload, `FinalizeStartingRelics()`, `Launch()`). Read §5c for the sequence that's actually in `DojoLaunch.cs` today.

**Q1 — launch from a controlled context: YES.** `EnterRoomDebug` is exactly what the `fight <id>` dev-console command calls (`FightConsoleCmd.cs`); combat entry within a run is one call. It builds `CombatRoom` → `CombatState(encounter, runState, modifiers, badges, mpScaling)`, adds `runState.Players`, generates monsters, transitions into the combat scene. **The red-flag scenario is avoided:** `RunState.CreateForNewRun` builds a synthetic run with default acts, and `EnterRoomDebug` uses a `MockSinglePointActMap` — no real map graph and no serialized persistent player are required.

**Q2 — persists nothing: YES, by construction.** `shouldSave` sets `RunManager.ShouldSave`, which gates every persistence path: mid-run save (`RunSaveManager.cs:75`) and **the entire end-of-run block** — `CreateRunHistoryEntry`, `UpdateProgressWithRunData`, achievements, metrics upload — is wrapped in `if (ShouldSave)` inside `RunManager.OnEnded(isVictory)` (~line 1281). `CleanUp()` re-asserts `ShouldSave = false` and nulls `State`. The *only* unconditional `CreateRunHistoryEntry` calls are the main-menu "recover orphaned/abandoned run on startup" path — never reached by our flow. (Still confirm empirically — static analysis can't see a stray cloud/telemetry side-channel.)

**Q3 — clean return to Run History: YES, existing machinery.** Architectural key: **Run History is a submenu on the main-menu scene** (`NMainMenuSubmenuStack` → `PushSubmenuType<NRunHistory>()`), not part of a run. Return = `NGame.ReturnToMainMenu()` (used by pause-menu "quit run" + game-over screen): `FadeOut → RunManager.CleanUp() → LoadMainMenu() → SetCurrentScene(NMainMenu)`, then re-push the RunHistory submenu. **"Try again"** = skip `ReturnToMainMenu`, call `CleanUp()`, re-run the launch sequence with the same reconstructed loadout.

**UI hook:** `NMapPointHistoryEntry : NClickableControl` (has a `FloorNum` property) is the clickable floor-icon — where the confirm-dialog → launch handler attaches.

**Rung-1 field contract (what combat reads to init = the reconstructor's output contract).** From `CombatState` / `CombatRoom` / `CreateCreature`:
- **Encounter** `EncounterModel` (from `model_id`) + generated monsters (`monster_ids`)
- **Ascension** → monster HP rolls (`SetUniqueMonsterHpValue` via `RunState.Rng.Niche`, HP scaling)
- **Per-Player**: deck (CardModels), relics (`player.Relics`, IsMelted-filtered), potions (`player.PotionSlots`), HP (on `player.Creature`), gold, character
- **Run-level**: `Modifiers`, `BadgeModels`, `MultiplayerScalingModel` (null SP); `RunState` services `Rng` / `CurrentActIndex` / `CurrentMapCoord`

This maps 1:1 onto the §5 recoverability table — no hidden fields combat demands.

**Reconstruction-phase note (not a spike blocker):** for *exact* enemies you must override `Encounter.GenerateMonstersWithSlots` (it generates a monster set; you want to force the logged `monster_ids`). A fight launching at all clears the spike bar.

> Key decompiled anchors (build-specific, re-confirm against the live build): `Core/Combat/CombatState.cs`, `Core/Rooms/CombatRoom.cs`, `Core/Runs/RunManager.cs` (`SetUpNewSingleplayer`/`OnEnded`/`CleanUp`/`EnterRoomDebug`), `Core/Nodes/NGame.cs` (`StartNewSingleplayerRun`/`ReturnToMainMenu`), `Core/Nodes/Debug/NSceneBootstrapper.cs`, `Core/DevConsole/ConsoleCommands/FightConsoleCmd.cs`, `Core/Nodes/Screens/RunHistoryScreen/`.

### 8.1 Original spike plan (below) — superseded by 8.0 except for the empirical rung-3 step

**The entire approach is dead if you can't launch a combat in isolation and tear it down without touching the real save.** The spike answers three yes/no questions: (Q1) can you start a combat from a context you fully control, (Q2) can you guarantee it persists nothing, (Q3) can you return to Run History cleanly. Strategy: instrument how the game *itself* enters combat, then replicate that from a throwaway context.

### Setup the instruments first (~half a day)
1. **Attach a debugger.** Put `steam_appid.txt` containing `2868840` in the StS2 dir; make a `.NET Executable` run config in Rider pointing at the game exe; enable **external source debugging** (Rider → Settings → Build, Execution, Deployment → Debugger → .Net Languages → "Enable external source debug"). Copy your `.pdb` next to the mod dll (add a `<Copy>` to the `CopyToModsFolderOnBuild` target).
2. **Dev console.** With mods enabled, open with `` ` ``/`~`/`*`/`'`/`Shift+8`; `help` lists commands. Find the spawn-an-encounter command — it's your probe trigger.
3. **Logs.** Enable Mod Configurations → BaseLib → "Open log window on startup". Mac log/save dir: `~/Library/Application Support/SlayTheSpire2/`.
4. **Decompile** the game assemblies (see wiki Decompiling page). Anchor on the `CombatState` type (combat is organized around it — see Common Commands Cookbook). Grep for where `CombatState` is constructed and where the game transitions into the combat scene.

### The experiment ladder (each rung answers one question)
1. **Observe the real entry path (Q1).** Breakpoint where `CombatState` is constructed / the combat scene is entered. Trigger a fight via map or console. Walk the call stack and record **every field combat reads to initialize** (deck, relics, hp, potions, ascension, encounter→monster spawns). This list becomes the reconstructor's output contract. Highest-leverage hour in the project.
2. **Replicate from a synthetic context (the real Q1 test).** Add one dev-console command to the mod that builds a throwaway run/player with hardcoded junk values and calls the same entry path — bypassing the map. Strategy (a): swap the throwaway into the global "current run" slot, run combat, swap the real one back. (Strategy (b), heavier: start an actual throwaway seeded run and inject state — fall back to this only if (a) fails.) Iterate on log exceptions until a hardcoded fight launches and is playable.
3. **Prove nothing persists (Q2, empirically).** Hash + record mtimes of the save dir before. Launch the fight, **win** it. Diff: any `.run` changed? new history entry? rewards granted? map advanced? Repeat, **lose** it. If the save dir is byte-identical after both, Q2 is green. If something wrote, breakpoint the save/serialize and reward-grant paths to find who invoked them, and suppress/redirect to a throwaway.
4. **Prove clean return (Q3).** From the synthetic combat, on **both** win and loss, force a scene transition back to the Run History screen (bypassing the normal combat→victory→map flow). Find the Godot scene/screen manager in decompiled source. Confirm Run History renders intact and "try again" relaunches without a restart.

### Exit criteria
- **Green light (Approach A validated):** a hardcoded fight launches from a throwaway context, plays to a win and a loss, leaves the save dir byte-identical both times, and returns to Run History both times with "try again" working.
- **Red flags (reshape the plan):** combat can't init without a substantially-real run (map graph + serialized persistent player), OR winning unavoidably triggers a save-write/reward-grant you can't suppress. If so, pivot from "fake a context + launch combat" to "start a real throwaway run, inject reconstructed state, sandbox the persistence." Better to learn this now than after building the reconstructor.

**Timebox: 2–3 focused days.** Deliverable is not polished code — it's a one-paragraph verdict on the four exit criteria, plus the §rung-1 list of fields combat reads (the reconstructor's input contract).

> The combat-entry and scene-transition method/type names are build-specific (and may differ between v0.106 and v0.107), so get them firsthand via the rung-1 breakpoint walk rather than from memory.

---

## 9. Roadmap (durable — the task tool resets; trust this list)

**Done:** ✅ §8 spike (static + empirical) · ✅ dev environment + build/test loop · ✅ macOS Sentry hang fix · ✅ `dojo <id>` launch command (scaffold with junk loadout) · ✅ **reconstructor** (`.run` + floor N → provenance-tagged loadout; `dojoreplay <run_file_path> <floor>` command; empirically validated — see §0 and §5a for build notes and the bugs it surfaced) · ✅ **RunReconstructor + decision-helper unit-test harness** (8 fixture tests + 5 decision-helper groups; see §5b) · ✅ **relic-visibility fix + combat-end interception** (`DojoLaunch` restructured, Harmony patches for win/loss + secondary-write suppression, `dojoagain`; empirically confirmed in-game — see §0 and §5c) · ✅ **potion reconstruction** (structural replay + verified event-only grant/removal cases, wired into `dojoreplay`/UI launch; 8 acceptance fixtures — see §5d) · ✅ **Dojo entry point + real-profile run browser + Completion screen** (PR #3; main-menu Dojo button, re-used `NRunHistory` over real profile, combat-floor confirm+launch, Try Again / Return to Dojo / Return to Main Menu; full in-game smoke pass done — see §5e) · ✅ **live eligibility/resolve pass** (`LiveDojoContentResolver` wired into `DojoReplayLauncher`, refuses fights with unresolvable content; see §5f) · ✅ **ineligible floors greyed out in the run browser** (`DojoFloorEligibility`, cheap throwaway-`RunState` preview reconstruction; see §5g) · ✅ **custom Dojo run-browser screen + in-row combat view** (in-game smoke-tested, PASSED 2026-07-03; see §5i/§5j/§5l) · ✅ **"Dojo — Replay Setup" modal** (relic/card state editor, PR #12; in-game smoke-tested, PASSED 2026-07-04; see §5n).

**Next, in rough order:**
1. ~~Reconstructor~~ — done, see §5a.
2. ~~Intercept combat-end~~ — **done, see §5c/§5e.** Harmony patches skip the rewards screen/map/game-over screen on a Dojo win/loss and show `DojoCompletionScreen`; `dojoagain` still adds a dev-console "try again" path. Also bundled the relic-icon fix from §5a point 6, as planned (`DojoLaunch` restructured off `NGame.StartNewSingleplayerRun` onto the raw §8.0 sequence).
2b. ~~Replace the Run-History redirect with a Completion screen~~ — **done, see §5e.** `DojoCombatEndInterceptor` now shows `DojoCompletionScreen` as a modal on top of combat. Buttons: **Try Again** (`DojoLaunch.TryAgain()`), **Return to Dojo** (`ReturnToMainMenu()` then `NDojoScreen.Open(game)`), **Return to Main Menu** (`ReturnToMainMenu()` only).
3. ~~Suppress secondary writes~~ — **mostly done, see §5c.** `progress.save` (both the newly-found win-path leak and the previously-known loss-path leak) and `replays/latest.mcr` are patched/suppressed for Dojo runs. `prefs.save` traced to not actually be caused by combat end — left unpatched, flagged if it resurfaces.
4. ~~Eligibility/resolve pass~~ — **done, see §5f.** `Reconstruction/LiveDojoContentResolver.cs` is a live `ModelDb`-backed `IDojoContentResolver`; `DojoReplayLauncher` calls `DojoContentEligibility.Validate(...)` right after reconstruction (before any player mutation) and throws `DojoContentEligibilityException` on a miss, which the existing mutate-failure recovery path in `DojoLaunch` cleans up. Doubles as the reconstruction validator per §6. `DojoConsoleCmd` (`dojo <id>`) untouched — no reconstructed loadout to validate there.
5. ~~UI: Dojo entry point + run browser~~ — **first pass done (§5e); custom browser built (§5i); in-row combat view + full flag-flip removal done (§5j, 2026-07-03).** Main-menu Dojo button opens the custom `NDojoScreen` (sidebar filters, search, sort, per-act boss/elite pills); each row now **expands in place to the full per-act floor map** (reusing the stock `NMapPointHistoryEntry` widget), which replaced the old "View All Combats" drill-in to the stock `NRunHistory` — and with it the mod's last `IsRunningModded` flip. The one-run-at-a-time paging complaint is resolved. Remaining polish:
   - ~~Grey out ineligible floors~~ — **done, see §5g/§5j** (custom-screen pills and the in-row floor map both gated by `DojoFloorEligibility`; in-row map greys only ineligible COMBAT floors).
   - ~~In-game smoke test of the custom screen + in-row combat view (§5i/§5j)~~ — **done 2026-07-03 (§5l).** Full interaction flow (row expand → floor map → click-to-replay → completion → collapse/multi-expand/wide-act scroll → ESC) AND the §5h regression check (relaunch unmodded, real profile intact) both passed in-game.
   - **Deprioritized:** no explanatory tooltip for *why* a floor/pill is greyed out; richer error states for load failures / unsupported schema / modifier / multiplayer runs (the custom screen shows only an aggregate "N hidden" count; `RunHistoryFileSelection` already models the categories). Low-value UI polish — revisit opportunistically, not the next thing to build.
6. **Fast-follow** — ~~loadout editor over remaining `assumed` fields (relic counter/prop state)~~ **built as the "Dojo — Replay Setup" modal, merged via PR #12, in-game smoke test PASSED 2026-07-04 (§5n).** ~~Repeatable-seed fight sharing~~ — **DONE: implementation merged (PR #15, branch `replayable-seed`), in-game smoke test PASSED (export + import + same-seed determinism, 2026-07-04/05) — see §12i.** ~~§12h fast-follows (edit/delete saved fights, filtered Saved Fights list)~~ — **DONE, merged PRs #16/#17/#18 (§12j): export split, always-loaded rich rows, per-row edit/delete/copy, Submit + highlight, search + character/ascension filters, author field.** Remaining: make modded Sentry fully silent (`TODO(telemetry)` in `SentryMacOsHangPatch.cs`); optionally upgrade the saved-fight row to the full read-only Replay-Setup-style preview (§12h — the row is a rich always-loaded summary today, not the stateful modal editor).

## 10. Open questions to resolve during build
- ~~v1 potions: ship empty slots, or attempt potion-replay immediately?~~ **Resolved: potion replay implemented (2026-07-01), see §5d.** `DojoReplayConsoleCmd`'s mutate callback now grants the reconstructed potions. A live `ModelDb`-backed name resolver (vs. the current static table) remains a fast-follow (§9 item 6).
- ~~Which `room_type` values exactly count as combat?~~ **Resolved: `monster`/`elite`/`boss`.** Confirmed against `SCHEMA.md`'s full enumeration (`room_type ∈ {monster, event, rest_site, elite, treasure, boss, shop}`) and covered by the combat-floor-detection tests in §5b (event-combat floors are still found by `monster_ids` presence, not `room_type`, per §6).
- ~~Daily-run / modifier-bearing runs — in or out of v1?~~ **Resolved: out.** `RunHistoryFileSelection.cs`'s `ExcludeModifierRuns` defaults to `true`; modifier-bearing runs are classified `RunHistoryFileDecision.ModifierRun` and should be excluded/hidden by any custom browser or richer error-state UI (§9 item 5 follow-ups). The current first-pass `NRunHistory` reuse also filters floor-click replay eligibility so modifier-bearing runs cannot launch.
- **Test TODOs:** add a negative fixture for multiplayer replay/file filtering using `1782788638.run`; add live `ModelDb` integration coverage for `LiveDojoContentResolver` itself (today only the pure `DojoContentEligibility` logic is unit-tested, via `FakeContentResolver` — §5f) if a safe non-Godot way to load `ModelDb` content ever appears; add `RunHistoryLoader`/`MigrationManager` integration coverage if a safe non-Godot load path appears; add false-merchant Foul Potion coverage if the corpus turns up a real fixture (current scan found merchant-shop usage but no `FOUL_POTION` + `EVENT.FAKE_MERCHANT`/trial-merchant overlap); add try-again immutability/retry-seed tests once replay requests are modeled.
- ~~**Run-browser UX (future, post-v1):** one-run-at-a-time paging friction.~~ **Resolved (2026-07-02): the custom multi-run browser is built — see §5i.** Scrollable/searchable/filterable list with sort by date/floor/ascension, backed by direct real-profile `.run` file reads, exactly as this note anticipated.

## 11. References
- Template wiki: Setup, Modding Basics, Testing and Debugging, Decompiling, Extracting Assets and Text, Common Commands Cookbook, Things to Note — `https://github.com/Alchyr/ModTemplate-StS2/wiki`
- `runfiles/SCHEMA.md` (this project) — authoritative data-model reference.
- Sample runs are contained in `./runfiles/*.run`
- Decompiled game source in `./decompiled/sts2/` — primary anchor for combat-entry / scene / save code paths (see §8.0).

---

## 12. Repeatable-seed fight sharing — design spec (drafted 2026-07-04) + as-built (§12i/§12j)

> **STATUS: BUILT, MERGED, and in-game validated.** §12a–§12h below are the original design spec; the as-built truth is in **§12i** (core: capture/codec/gate/launch) and **§12j** (Saved Fights UI, which reworked several §12e/§12g decisions — explicit Submit instead of paste-to-import, richer rows, edit/delete/filters, author). Where the spec and §12j disagree, §12j wins.

**Goal:** let a player capture a specific Dojo fight setup — loadout, relic/card state, AND the exact RNG that drives it — and share it so someone else gets a bit-for-bit identical experience (same opening hand, same shuffles, same enemy intents/targets). This is a distinct feature from §5's reconstruction contract: §5 is retroactive (rebuild an approximate loadout from a lossy historical `.run` log; exact RNG is provably unrecoverable there, §5/§10). This feature is **forward, live-capture only** — it reads real RNG state directly out of a live `RunState` at the moment of capture, which the historical log never stores but a running game process always has. No name is needed for the feature itself; UI copy just describes the action ("Load a Fight", "Export").

**v1 scope, decided:**
- **Pre-combat capture only.** Freeze state right before the fight begins; no mid-combat freeze-frame (pausing partway through a fight and exporting the live board — piles, monster HP/intents/buffs, orbs, turn state). The base game's own save format has zero support for that (`Saves/SerializableRun.cs`, `Saves/Runs/SerializableRoom.cs` carry no combat-internal state at all — the game can only checkpoint between rooms), so it would mean inventing a full combat-state serializer from scratch. Deferred indefinitely, not just to a later phase.
- Single-fight only, same as the rest of Dojo (§2) — never a multi-floor/gauntlet export.
- Refuse import on game-build or payload-schema mismatch (see Compatibility gate below, **revised 2026-07-04**) rather than best-effort.
- **Try Again on a seed-captured fight replays the SAME seed+counters (decided 2026-07-04):** for an imported fight (or a fight exported then retried from the Completion screen), every attempt restores the captured RNG snapshot — identical opening hand/intents each try, the "repeatable" in the feature name. Normal uncaptured Dojo fights keep today's fresh-RNG-per-attempt behavior (§3) unchanged.

### 12a. Capture mechanism — one shared routine, three entry points

The exact snapshot point already exists in the current pipeline: inside `DojoReplayLauncher.LaunchReplay`'s `mutate` callback (`DojoReplayLauncher.cs:75-181`), right after `adjustments?.ApplyTo(loadout)` (line 104) and the eligibility gate, the method already has, as local variables, everything an export needs:
- The fully-materialized `loadout` (deck/relics/potions/HP/gold, relic/card `Props` already stamped by any Replay Setup modal adjustments) — no new reconstruction needed, it's the same `ReconstructedLoadout` already being applied to the player.
- `runState` — whose `runState.Rng` (a `RunRngSet`) and each player's `player.PlayerRng` (a `PlayerRngSet`) are live, real, already-seeded RNG sets for this specific launch. Both already round-trip through the game's own save format (`RunRngSet.ToSerializable()`/`FromSave()`, `RunRngSet.cs:71-97`; `Player.cs:297` shows the equivalent for `PlayerRngSet`) — this is the identical mechanism the base game uses for quit/resume, just read off instead of written to disk.

At this point in `LaunchInternal` (`DojoLaunch.cs:68-148`), `FixActForEncounter` (line 113, before `mutate` runs) has only touched the `UpFront` stream (map/room generation) — every combat-relevant stream (`Shuffle`, `CombatCardGeneration`, `CombatCardSelection`, `CombatEnergyCosts`, `CombatTargets`, `MonsterAi`, `CombatOrbGeneration`, `CombatPotionGeneration`, `Niche`) is still at its fresh starting counter. So capturing `runState.Rng.ToSerializable()` + each player's `PlayerRng.ToSerializable()` right there, alongside the loadout, is sufficient to reproduce the exact draw order and everything downstream — nothing about card order needs to be (or can cleanly be) stored explicitly; the same deterministic shuffle regenerates it from the counter on import.

**Confirmed safe:** the Replay Setup modal's Random relic/card preset uses `Random.Shared.Next(...)` (`DojoReplaySetupModal.cs:658`), not any run/player RNG stream — fiddling with Zero/Random/Primed presets before exporting never perturbs the streams being captured. The exported relic/card `Props` already bake in whatever value a preset landed on; nothing needs to be re-rolled on import.

**Determinism gaps found by pre-implementation code review (2026-07-04, verified against `decompiled/`) — the payload alone is NOT sufficient; both must be handled:**

1. **Encounter monster composition is outside `RunRngSet`/`PlayerRngSet`.** `DojoLaunch.cs:85` calls `encounter.DebugRandomizeRng()`, which seeds a **private RNG on the `EncounterModel` from wall-clock time** (`EncounterModel.cs:288-292`); that RNG drives `GenerateMonsters()` — which variants/lineup a randomized encounter rolls. Two importers of the same payload would get different enemy compositions. **Fix (decided):** capture-enabled launches skip `DebugRandomizeRng()` — with `_rng` left null, `GenerateMonstersWithSlots` derives its seed deterministically from the run seed (`EncounterModel.cs:211`: `runState.Rng.Seed + TotalFloor + hash(encounterId)`), so composition reproduces for free with no new payload field. Everything else combat-relevant was traced onto the run streams and is covered by the seed: deck shuffle (`CombatManager.cs:241`, `Shuffle`), monster HP rolls (`CombatState.cs:138`, `Niche`), monster AI (`MonsterAi`), each monster's private `Rng` (`CombatState.cs:142`, derived from `RunState.Rng.Seed`), and `PlayerRngSet` itself (`Player.cs:240`, derived from the run string seed — the payload's player-RNG section is redundant-but-harmless robustness).
2. **Unlock-state-dependent generation pools break cross-user identity.** In-combat random card generation filters its candidate pool by the owner's unlock progress — e.g. `Discovery.cs:27`: `CardPool.GetUnlockedCards(base.Owner.UnlockState, ...)`. Same seed, same counter, different unlocks → different generated card. `DojoLaunch` currently builds runs from `SaveManager.Instance.GenerateUnlockStateFromProgress()` (line 88), which differs per user. **Fix (decided 2026-07-04): capture and import launches both use a fully-unlocked `UnlockState`,** making pools identical for everyone; the throwaway run never saves, so nothing leaks to real progress. Accepted trade-off: a shared fight can generate cards the importer hasn't unlocked yet (mild spoiler). Regular non-captured Dojo launches keep using real progress, unchanged.

**Import-path constraint (state it now so the implementation doesn't fight it):** `RunRngSet.LoadFromSerializable`/`PlayerRngSet.LoadFromSerializable` both **throw on seed mismatch**, and `RunState.Rng` is `{ get; init; }` — so the import launch must construct the throwaway run with the payload's seed string from the start. `DojoLaunch.LaunchInternal` hardcodes `SeedHelper.GetRandomSeed()` (line 87) and needs a seed-override parameter alongside the `PrepareOnly` mode noted above. After construction, applying the payload's counters via `LoadFromSerializable` reconciles any residual drift (e.g. starting-relic hooks that consumed a stream pre-capture).

**Two more implementation notes from the same review:**
- **Snapshot retention must live in `LaunchInternal`, not `LaunchThrowawayRun`** — `TryAgain()` calls `LaunchInternal` directly and every attempt reseeds, so the retained snapshot (the `_lastRequest` sibling) must be refreshed per attempt or the Completion-screen Export exports a stale attempt's RNG.
- **`PrepareOnly` should skip more than scene creation:** `LaunchInternal` stops menu/run music at the top and runs heavy `PreloadManager` asset loads — a pure "Export without playing" should do neither.

Three entry points share this one capture point:
1. **Real "Start" on `DojoReplaySetupModal`** — normal play, unchanged. The snapshot captured during this launch is retained in memory (a new sibling to `DojoLaunch`'s existing `_lastRequest` field) so the Completion screen can offer to export it after the fact, win or lose.
2. **New "Export" button on `DojoReplaySetupModal`** (the "capture on the initial fight selection screen" ask) — runs the identical prepare-through-`mutate` sequence, then instead of `game.RootSceneContainer.SetCurrentScene(NRun.Create(runState))` (`DojoLaunch.cs:130`), tears down via `RunManager.Instance.CleanUp()` — the same call already used on the failure path (`DojoLaunch.cs:145`) — and hands the snapshot to the packaging step. No combat is ever entered. **Implementation note:** `DojoLaunch.LaunchThrowawayRun`/`LaunchInternal` need a new mode (e.g. an `enterScene: bool` flag or a parallel `PrepareOnly` method) to stop before scene creation instead of always proceeding — not yet built.
3. **New "Export" button on `DojoCompletionScreen`** — reuses the snapshot already captured by entry point 1 for the attempt that just concluded; no recapture logic needed.

### 12b. Payload

- Character, ascension, encounter id.
- Loadout: deck (card id + upgrade level + enchantment), relics (id + `Props`), potions, HP, gold — the same DTOs `DojoStateAdjustments`/`ReconstructedLoadout` already produce.
- Seed string + full RNG counters: `SerializableRunRngSet` (all `RunRngType` counters) + each player's serialized `PlayerRngSet`.
- Metadata: title, free-text comment, created date. No account/author system — if attribution matters it's just another free-text field the exporter fills in.
- Compatibility fields: game `build_id`, this mod's version (schema_version for the payload itself, so the format can evolve independently of those two).

### 12c. Compatibility gate (refuse on mismatch) — revised 2026-07-04

Distinct from, and in addition to, the existing `DojoContentEligibility` content-resolve gate (§6/§5f) — content can resolve fine (same card/relic ids exist) while the shuffle/RNG-consumption logic has still changed between builds, silently breaking the "identical experience" promise without any missing-content error to catch it. On import, compare game `build_id` **exactly** and the payload's own `schema_version` **exactly**; either mismatch refuses the import with a clear message, before the content-eligibility check even runs. **The mod version is recorded in the payload for diagnostics but does NOT gate** (decided 2026-07-04, revising the original draft): fight determinism is driven by game code, so exact mod-version matching would orphan every previously shared fight on every mod release while adding no real safety — format changes are already covered by the payload `schema_version`.

### 12d. Transport

Two encodings of the same payload:
- **File** — canonical JSON, same style as the rest of this mod's save-adjacent formats.
- **Compact code** — the payload run through the game's own `IPacketSerializable`/`PacketWriter` binary encoding (already used for `SerializableRun` and network sync — reuse it rather than hand-rolling a binary format), then gzip + base64. **Expectation to set:** because the payload carries a full deck + relic list (not just a seed), this is a long paste-able blob, not a short human-typable code like a classic StS seed string.

**Open consideration (flagged 2026-07-04, decide at implementation time):** two encodings of one payload are two serializers that must never disagree. Since the compact code is a long blob either way, gzip+base64 of the canonical JSON would collapse this to a single format at maybe 2-3× the paste length — weigh that against the `PacketWriter` reuse before committing.

### 12e. Import UX — "Load a Fight"

A single text input, greyed-out placeholder text ("Paste the contents of an exported fight to replay it"), overwritten the moment the player pastes into it. No separate "import" button — pasting triggers validation immediately:
1. Base64 + gzip decode, then packet-decode into the payload shape. Malformed input → clear inline error, never a raw exception/stack trace.
2. Compatibility gate (12c) — refuse with a clear message on any build/mod-version mismatch.
3. `DojoContentEligibility` content-resolve gate (§6/§5f) — refuse if any card/relic/encounter id doesn't resolve against currently-loaded content.
4. On success: auto-saved into the local "Saved Fights" library (12g) and immediately opens the same summary-strip preview the Replay Setup modal already shows (encounter/character/ascension + HP/Gold/Potion chips, relic/card state rows) — **shown read-only**, not editable, since the whole point is reproducing exactly what was captured. Start/Cancel from there.

### 12f. Storage

A new, mod-owned directory that never derives from or overlaps with `UserDataPathProvider`'s profile-scoped paths (`GetProfileScopedPath`/`GetProfileDir`) — built independently of that helper entirely, the same way `RealProfilePath.cs` was deliberately kept out of the `IsRunningModded` flip (§5i/§5j/§6). Global to the mod install, not per-real-profile (a "Saved Fight" isn't tied to any one profile). Exact path TBD at implementation time; the constraint is just "structurally impossible to confuse with anything `SaveManager` reads or writes."

### 12g. Browsing — a second, separate list

**"Saved Fights"**, a flat list distinct from the existing Run History browser (`NDojoScreen`/`DojoRunRow`, §5i/§5j) — deliberately separate rather than folded into it, since these are individual captured/imported fight setups, not full multi-floor runs: no per-act floor map, no floor-by-floor drill-in, just one row per saved fight. **UI home (decided 2026-07-04): a Runs / Saved Fights tab-style toggle on the existing `NDojoScreen`** (one screen, reusing its chrome and sidebar filter/sort pattern), with the "Load a Fight" paste box (12e) at the top of the Saved Fights view. Each row: title, comment, character, ascension, encounter name, created date, and a badge distinguishing "created by you" vs. "imported." Reuses the filter/sort/search pattern already proven in `DojoRunListQueries` (§5i) against this much simpler schema. Clicking a row opens the same read-only summary-strip preview as a fresh paste (12e) with Start/Cancel.

### 12h. Fast-follow (was "explicitly NOT in v1" — MOSTLY DONE, see §12j)

Originally deferred: editing metadata (rename title/re-comment) and deleting saved fights (v1 only created and read entries); sidebar character/ascension filters (v1 shipped unfiltered, newest-first + paste box only); and upgrading the inline Start/summary section to a full read-only Replay-Setup-style preview. **The first two shipped 2026-07-05 (§12j, PRs #16–#18): edit + delete + character/ascension/search filters are all in.** Still open: the full read-only Replay-Setup-style preview (the row is now an always-loaded rich summary — avatars, relic bar, fight numbers — but not the modal's stateful relic/card editor).

### 12i. As-built notes (2026-07-04) — implementation COMPLETE, in-game smoke test PASSED

Built in one session on branch `replayable-seed`, phases tracked in the out-of-repo work log `../SEED_SHARING_PROGRESS.md` (kept outside the repo deliberately — see it for the full commit-by-commit log, the verified code-fact list, and the smoke checklist). All unit tests pass (42 PASS lines total, 10 of them new seed-sharing groups); every phase build-verified. **Smoke round 1 (2026-07-04) failed both export paths and both causes are fixed (commit fff229c): the trimmed-runtime System.Text.Json crash (risk #1, materialized exactly as predicted — codec JSON layer now hand-rolled, see the SharedFightCodec bullet below) and the prepare-only asset-preload loading overlay tearing down the Replay Setup modal (prepare-only now skips preloads + NetLoadingHandle). Re-test round 2 (2026-07-04): both export paths CONFIRMED WORKING in-game** (user-verified). **Import/paste + same-seed determinism smoke test PASSED (2026-07-05, user-verified) — the §12 loop is now fully validated end-to-end in-game.**

> **⚠️ The Saved Fights UI described in the file list below has since evolved — see §12j (2026-07-05, PRs #16–#18) for the current state.** In particular the export flow (single "Export Fight Code" → split Copy/Save), the row layout (paste-to-import + expand-in-place + BY YOU/IMPORTED badges → explicit Submit + always-loaded rich rows + edit/delete/copy actions + author tag), and the sidebar (unfiltered → search + character/ascension filters) are all superseded by §12j. The determinism/codec/capture core (`DojoLaunch`, `SharedFight*`, `DojoFightSnapshot`, `DojoLoadoutApplier`) is unchanged.

**What shipped, by file (all under `STS2DojoCode/`):**
- `DojoLaunch.cs` — `DojoLaunchOptions` (seed override + RNG-counter restore, retained in the Try Again request), per-attempt `DojoFightSnapshot` capture post-mutate (`LastSnapshot`), `PrepareSnapshot` (prepare-only mode: full sequence through capture, no scene, `CleanUp()`; refuses if a run is in progress). **Two §12a determinism fixes: `DebugRandomizeRng()` call removed** (encounter composition now derives from the run seed via `EncounterModel.cs`'s null-`_rng` fallback) **and `UnlockState.all` replaces `GenerateUnlockStateFromProgress()`** (unlock-filtered generation pools identical for all players). Note the deliberate widening vs. the original decision wording: ALL throwaway launches (incl. `dojo`/`dojoreplay` console commands) are capture-ready, because the Completion screen offers Export after any fight — there is no "non-captured" launch.
- `SeedSharing/DojoFightSnapshot.cs` — snapshot + options DTOs (test-compiled).
- `SeedSharing/SharedFightPayload.cs` — §12b payload, schema v1, `FromSnapshot`/`ToLaunchOptions`/`GetStructuralProblems`.
- `SeedSharing/SharedFightCodec.cs` — **§12d's open consideration resolved: ONE format.** Canonical JSON file form + `STS2DOJO1.` + base64(gzip(same JSON)) paste code; `Parse()` accepts either; all decode failures are `SharedFightFormatException` with presentable messages. **The JSON layer is HAND-ROLLED over `Utf8JsonWriter`/`JsonDocument` — do NOT "simplify" it back to `JsonSerializer`:** the original reflection-resolver version crashed in-game (smoke round 1, 2026-07-04 — the game ships a self-contained resource-trimmed .NET 9.0.7; log signature "Undefined resource string ID"). Wire format (snake_case names + enum dict keys, ModelId strings, SavedProperties name/value field shape) is locked by tests.
- `SeedSharing/SharedFightGate.cs` — §12c revised gate (game build exact + payload schema exact; mod version diagnostics-only).
- `SeedSharing/DojoFightLibrary.cs` — §12f/§12g storage: `user://sts2dojo/fights/` (the mod's existing sts2dojo/ home; structurally disjoint from all profile-scoped paths), one JSON file per fight, origin (`created-`/`imported-`) in the filename prefix, temp+move writes, damaged entries counted not thrown.
- `SeedSharing/SharedFightExporter.cs` — snapshot → payload (build id from `ReleaseInfoManager`, `ModVersion` const **"v0.0.0" — keep in sync with STS2Dojo.json**, auto title "Encounter — date") → library + clipboard. The only seed-sharing class touching Godot/game singletons.
- `SeedSharing/SharedFightLauncher.cs` — import refusal chain (compat gate → live content resolve incl. character) + launch with payload seed/counters pinned.
- `DojoLoadoutApplier.cs` — the subtle apply-loadout-to-player sequence extracted from `DojoReplayLauncher` so replay and import share it (all §5a-bug comments preserved there).
- `DojoReplayLauncher.cs` — mutate builder extracted (`BuildMutate`, shared for launch/capture parity); new `PrepareReplaySnapshot`.
- `DojoCompletionScreen.cs` — 4th button "Export Fight Code" (§12a entry point 3; re-press re-copies instead of duplicating); panel 540→620 tall.
- `DojoReplaySetupModal.cs` — "Export Fight Code" `DojoPresetChip` in the action bar (§12a entry point 2; NOT a third `NPopupYesNoButton` — `IsYes` re-registers confirm/ESC hotkeys). Chip label doubles as live status; `Configure` can't be re-run (it stacks labels), so `SetExportChipText` mirrors its sizing.
- `DojoScreen.cs` + `DojoSavedFightsView.cs` — Run History ↔ Saved Fights sidebar toggle on `NDojoScreen` (the §12g UI-home decision); paste box imports on paste (§12e, debounced, short fragments ignored); flat newest-first list with BY YOU/IMPORTED badges; **expand-in-place summary + Start section instead of 12e's "same read-only Replay-Setup preview" — a deliberate v1 downgrade** (the setup modal is deeply RunHistory-coupled; the inline section shows encounter/HP/gold/deck/relic/potion/seed numbers and re-checks both gates at expand time). Upgrading it is a 12h fast-follow.
- `Reconstruction/DojoContentEligibility.cs` — id-list `Validate` overload + new `DojoContentKind.Character` (an imported fight can name a modded character the importer lacks); wired into `LiveDojoContentResolver`.

**Test additions (§5b harness):** doubles for `SerializablePotion`/`SavedProperties`/both RNG sets + both RNG enums (members must stay byte-identical to decompiled), props fields on the card/relic doubles, and `SeedSharingRunner` (10 groups: snapshot mapping, JSON/code round-trips + wire-format lock, both-forms parsing, malformed inputs, structural validation, gate matrix, launch-options mapping, library filenames, library save/list round-trip).

### 12j. Saved Fights UI follow-ups (2026-07-05) — merged via PRs #16/#17/#18, in-game confirmed

The §12h fast-follow set plus an export-flow rework and an author field, built and merged in three PRs after the §12i core landed. **All in-game confirmed working (user-verified 2026-07-05).** Supersedes the Saved-Fights-UI parts of §12i (the determinism/codec core is untouched).

**Export flow split (PR #16).** The single "Export Fight Code" action (which did save+copy at once) is split into two explicit choices with click feedback:
- `SharedFightExporter` now exposes `CopyCode` (clipboard only) and `SaveToLibrary` (library only) over a shared private `BuildPayload`; the old combined `Export` is gone.
- `DojoCompletionScreen`: the one Export button → **Copy Fight Code** + **Save to Saved Fights** (now 5 event-option buttons; panel grew 620→716). Copy caches the code for re-press; Save guards against a duplicate entry.
- `DojoReplaySetupModal`: the one export chip → **Copy Code** + **Save Fight** chips (still `DojoPresetChip`s, not `NPopupYesNoButton`s — the §5n hotkey reason). A successful **Save** closes the modal and jumps to the Saved Fights tab (see nav below); Copy keeps the modal open.

**Save-from-modal navigates to the Saved Fights tab (PR #17).** `NDojoScreen.ShowSavedFightsTab()` (static, guarded on the `_instance`) → `SwitchToSavedFightsTab()` selects the Saved Fights mode chip and flips `SetMode(true)`. Fixes the old behavior where saving from the Replay Setup modal left the player on Run History.

**Saved Fights rows reworked (`DojoSavedFightsView`, PRs #16/#18).**
- **Always fully loaded** — the §12i expand-in-place Load toggle is gone; the launch action is a persistent **Replay Fight** chip. The "Same seed every attempt…" explainer line was dropped.
- **Rich identity per row** — a character avatar + `A{asc}` badge (`DojoUi.MakeCharacterToken`), a boss/elite/monster encounter avatar (resolved via `EncounterModel.RoomType` → `ImageHelper.GetRoomIconPath`, same as `DojoFightPill`), and the fight's relic bar (`payload.Relics` = the captured loadout, i.e. "up until then"). Fight-data block vertically centered between subtitle and row bottom.
- **Per-row actions**: Replay Fight, **Copy Code** (one-click clipboard), **Edit** (title/description), **Delete** (confirmation modal). A refused fight (gate fail at build time) shows the refusal in place of Replay.
- **Explicit Submit** — paste no longer auto-imports (§12e's paste-to-import is gone); a Submit button (or Enter) imports. The just-imported row gets a **gold highlight border** that clears on the next successful action (tracked by `_highlightedPath`, re-applied across rebuilds).

**Edit/delete plumbing.** New `DojoSavedFightModals.cs` — `DojoConfirmModal` (yes/no) + `DojoEditFightModal` (title/description LineEdits), both §5m-safe (root `Ignore` + script-less full-rect `Stop` `CenterContainer`, built imperatively, no lifecycle-override reliance, like the other modals). `DojoFightLibrary` gains `Overwrite` (edit, same path, temp+move) and `Delete`.

**Search + filters (PR #17).** A Saved Fights sidebar filter section (`BuildSavedFightsFilters`) mirroring the Run History widgets — search box + character + ascension chip grids, **no victory/sort**. `SetMode` shows the runs section OR the saved section. Filtering runs in memory over a cached listing: `DojoSavedFightsView` loads the library once per `Refresh`, precomputes a lowercased search haystack per entry, and `SetSearch`/`SetCharacterFilter`/`SetAscensionFilter`/`ResetFilters` just re-filter (never re-read disk). Search matches title/description/author/character/enemy/relic names + seed; status line shows "N of M match".

**Author field (PR #18).** `SharedFightPayload.Author` — the exporter's Steam display name, stamped once at export (`SharedFightExporter.CurrentAuthorName` via `PlatformUtil.PrimaryPlatform`/`GetLocalPlayerId`/`GetPlayerNameRaw`, empty/no-throw if unavailable), preserved through import/edit. Displayed as a gold "by {author}" tag and folded into the search haystack. **Added WITHOUT a schema bump** (still v1): the hand-rolled codec defaults missing fields to empty and ignores unknown ones, so older v1 codes/library files stay importable and older builds ignore the field. The `BY YOU`/`IMPORTED` origin badges were removed (the author tag conveys ownership; origin is still tracked in the entry/filename, just not shown). `FromSnapshot` gained an `author` param; tests lock the new wire field.
